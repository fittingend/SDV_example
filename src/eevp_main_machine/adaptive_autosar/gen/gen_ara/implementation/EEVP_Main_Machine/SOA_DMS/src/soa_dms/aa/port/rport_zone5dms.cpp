///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rport_zone5dms.cpp
/// SOFTWARE COMPONENT NAME           : RPort_Zone5Dms
/// GENERATED DATE                    : 2025-09-05 11:31:35
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "soa_dms/aa/port/rport_zone5dms.h"
 
namespace soa_dms
{
namespace aa
{
namespace port
{
 
RPort_Zone5Dms::RPort_Zone5Dms()
    : m_logger(ara::log::CreateLogger("SDMS", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverzone5DmsDeviceNormal{nullptr}
    , m_receiverzone5DmsDriverStatus{nullptr}
    , m_receiverzone5DmsStatus{nullptr}
{
}
 
RPort_Zone5Dms::~RPort_Zone5Dms()
{
}
 
void RPort_Zone5Dms::Start()
{
    m_logger.LogVerbose() << "RPort_Zone5Dms::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"SOA_DMS/AA/RPort_Zone5Dms"};
    auto handler = [this](ara::com::ServiceHandleContainer<zone5::dms::control::proxy::Zone5DmsProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = zone5::dms::control::proxy::Zone5DmsProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPort_Zone5Dms::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPort_Zone5Dms::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPort_Zone5Dms::Terminate()
{
    m_logger.LogVerbose() << "RPort_Zone5Dms::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribezone5DmsDeviceNormal();
        StopSubscribezone5DmsDriverStatus();
        StopSubscribezone5DmsStatus();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPort_Zone5Dms::Terminate::StopFindService";
    }
}
 
void RPort_Zone5Dms::Find(ara::com::ServiceHandleContainer<zone5::dms::control::proxy::Zone5DmsProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPort_Zone5Dms::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPort_Zone5Dms::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPort_Zone5Dms::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPort_Zone5Dms::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<zone5::dms::control::proxy::Zone5DmsProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        Subscribezone5DmsDeviceNormal();
        Subscribezone5DmsDriverStatus();
        Subscribezone5DmsStatus();
    }
}
 
void RPort_Zone5Dms::Subscribezone5DmsDeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverzone5DmsDeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->zone5DmsDeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Dms::Subscribezone5DmsDeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Dms::Subscribezone5DmsDeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_Zone5Dms::StopSubscribezone5DmsDeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->zone5DmsDeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPort_Zone5Dms::StopSubscribezone5DmsDeviceNormal::Unsubscribed";
    }
}
 
void RPort_Zone5Dms::RegistReceiverzone5DmsDeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldzone5DmsDeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->zone5DmsDeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Dms::RegistReceiverzone5DmsDeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Dms::RegistReceiverzone5DmsDeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_Zone5Dms::ReceiveFieldzone5DmsDeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->zone5DmsDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone5DmsDeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPort_Zone5Dms::ReadValuezone5DmsDeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_Zone5Dms::ReceiveFieldzone5DmsDeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_Zone5Dms::ReceiveFieldzone5DmsDeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_Zone5Dms::ReceiveFieldzone5DmsDeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->zone5DmsDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone5DmsDeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPort_Zone5Dms::ReadValuezone5DmsDeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_Zone5Dms::ReceiveFieldzone5DmsDeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_Zone5Dms::ReceiveFieldzone5DmsDeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_Zone5Dms::ReadValuezone5DmsDeviceNormal(ara::com::SamplePtr<zone5::dms::control::proxy::fields::zone5DmsDeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiverzone5DmsDeviceNormal)
    {
        m_receiverzone5DmsDeviceNormal(value);
    }
}
 
void RPort_Zone5Dms::RegistFieldHandlerzone5DmsDeviceNormal(std::function<void(const zone5::dms::control::proxy::fields::zone5DmsDeviceNormal::FieldType&)> handler)
{
    m_receiverzone5DmsDeviceNormal = handler;
}
 
void RPort_Zone5Dms::Getzone5DmsDeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->zone5DmsDeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Dms::Getzone5DmsDeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiverzone5DmsDeviceNormal)
            {
                m_receiverzone5DmsDeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Dms::Getzone5DmsDeviceNormal::" << response.Error().Message();
        }
    }
}
 
void RPort_Zone5Dms::Subscribezone5DmsDriverStatus()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverzone5DmsDriverStatus();
        
        // request subscribe
        auto subscribe = m_interface->zone5DmsDriverStatus.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Dms::Subscribezone5DmsDriverStatus::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Dms::Subscribezone5DmsDriverStatus::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_Zone5Dms::StopSubscribezone5DmsDriverStatus()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->zone5DmsDriverStatus.Unsubscribe();
        m_logger.LogVerbose() << "RPort_Zone5Dms::StopSubscribezone5DmsDriverStatus::Unsubscribed";
    }
}
 
void RPort_Zone5Dms::RegistReceiverzone5DmsDriverStatus()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldzone5DmsDriverStatusTriggered();
        };
        
        // regist callback
        auto callback = m_interface->zone5DmsDriverStatus.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Dms::RegistReceiverzone5DmsDriverStatus::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Dms::RegistReceiverzone5DmsDriverStatus::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_Zone5Dms::ReceiveFieldzone5DmsDriverStatusTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->zone5DmsDriverStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone5DmsDriverStatus.GetNewSamples([&](auto samplePtr) {
                RPort_Zone5Dms::ReadValuezone5DmsDriverStatus(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_Zone5Dms::ReceiveFieldzone5DmsDriverStatus::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_Zone5Dms::ReceiveFieldzone5DmsDriverStatus::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_Zone5Dms::ReceiveFieldzone5DmsDriverStatusCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->zone5DmsDriverStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone5DmsDriverStatus.GetNewSamples([&](auto samplePtr) {
                    RPort_Zone5Dms::ReadValuezone5DmsDriverStatus(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_Zone5Dms::ReceiveFieldzone5DmsDriverStatus::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_Zone5Dms::ReceiveFieldzone5DmsDriverStatus::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_Zone5Dms::ReadValuezone5DmsDriverStatus(ara::com::SamplePtr<zone5::dms::control::proxy::fields::zone5DmsDriverStatus::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiverzone5DmsDriverStatus)
    {
        m_receiverzone5DmsDriverStatus(value);
    }
}
 
void RPort_Zone5Dms::RegistFieldHandlerzone5DmsDriverStatus(std::function<void(const zone5::dms::control::proxy::fields::zone5DmsDriverStatus::FieldType&)> handler)
{
    m_receiverzone5DmsDriverStatus = handler;
}
 
void RPort_Zone5Dms::Getzone5DmsDriverStatus()
{
    if (m_found)
    {
        auto request = m_interface->zone5DmsDriverStatus.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Dms::Getzone5DmsDriverStatus::Responded";
            
            auto result = response.Value();
            if (m_receiverzone5DmsDriverStatus)
            {
                m_receiverzone5DmsDriverStatus(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Dms::Getzone5DmsDriverStatus::" << response.Error().Message();
        }
    }
}
 
void RPort_Zone5Dms::Subscribezone5DmsStatus()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverzone5DmsStatus();
        
        // request subscribe
        auto subscribe = m_interface->zone5DmsStatus.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Dms::Subscribezone5DmsStatus::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Dms::Subscribezone5DmsStatus::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_Zone5Dms::StopSubscribezone5DmsStatus()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->zone5DmsStatus.Unsubscribe();
        m_logger.LogVerbose() << "RPort_Zone5Dms::StopSubscribezone5DmsStatus::Unsubscribed";
    }
}
 
void RPort_Zone5Dms::RegistReceiverzone5DmsStatus()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldzone5DmsStatusTriggered();
        };
        
        // regist callback
        auto callback = m_interface->zone5DmsStatus.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Dms::RegistReceiverzone5DmsStatus::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Dms::RegistReceiverzone5DmsStatus::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_Zone5Dms::ReceiveFieldzone5DmsStatusTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->zone5DmsStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone5DmsStatus.GetNewSamples([&](auto samplePtr) {
                RPort_Zone5Dms::ReadValuezone5DmsStatus(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_Zone5Dms::ReceiveFieldzone5DmsStatus::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_Zone5Dms::ReceiveFieldzone5DmsStatus::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_Zone5Dms::ReceiveFieldzone5DmsStatusCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->zone5DmsStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone5DmsStatus.GetNewSamples([&](auto samplePtr) {
                    RPort_Zone5Dms::ReadValuezone5DmsStatus(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_Zone5Dms::ReceiveFieldzone5DmsStatus::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_Zone5Dms::ReceiveFieldzone5DmsStatus::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_Zone5Dms::ReadValuezone5DmsStatus(ara::com::SamplePtr<zone5::dms::control::proxy::fields::zone5DmsStatus::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiverzone5DmsStatus)
    {
        m_receiverzone5DmsStatus(value);
    }
}
 
void RPort_Zone5Dms::RegistFieldHandlerzone5DmsStatus(std::function<void(const zone5::dms::control::proxy::fields::zone5DmsStatus::FieldType&)> handler)
{
    m_receiverzone5DmsStatus = handler;
}
 
void RPort_Zone5Dms::Getzone5DmsStatus()
{
    if (m_found)
    {
        auto request = m_interface->zone5DmsStatus.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Dms::Getzone5DmsStatus::Responded";
            
            auto result = response.Value();
            if (m_receiverzone5DmsStatus)
            {
                m_receiverzone5DmsStatus(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Dms::Getzone5DmsStatus::" << response.Error().Message();
        }
    }
}
 
void RPort_Zone5Dms::RequestSetDmsPower(const std::uint8_t& on)
{
    if (m_found)
    {
        m_interface->SetDmsPower(on);
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace soa_dms
 
/// EOF