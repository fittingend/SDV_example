///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rporthvac.cpp
/// SOFTWARE COMPONENT NAME           : RPortHvac
/// GENERATED DATE                    : 2025-09-05 11:31:33
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "monitoringmanager/aa/port/rporthvac.h"
 
namespace monitoringmanager
{
namespace aa
{
namespace port
{
 
RPortHvac::RPortHvac()
    : m_logger(ara::log::CreateLogger("MONM", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverSoaHvacSwVersion{nullptr}
    , m_receiversoaHvacDeviceNormal{nullptr}
    , m_receiversoaHvacSetting{nullptr}
    , m_receiversoaHvacStatus{nullptr}
    , m_receiversoaHvacTemps{nullptr}
{
}
 
RPortHvac::~RPortHvac()
{
}
 
void RPortHvac::Start()
{
    m_logger.LogVerbose() << "RPortHvac::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"MonitoringManager/AA/RPortHvac"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::control::proxy::SoaHvacProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::control::proxy::SoaHvacProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPortHvac::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPortHvac::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPortHvac::Terminate()
{
    m_logger.LogVerbose() << "RPortHvac::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribeSoaHvacSwVersion();
        StopSubscribesoaHvacDeviceNormal();
        StopSubscribesoaHvacSetting();
        StopSubscribesoaHvacStatus();
        StopSubscribesoaHvacTemps();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPortHvac::Terminate::StopFindService";
    }
}
 
void RPortHvac::Find(ara::com::ServiceHandleContainer<eevp::control::proxy::SoaHvacProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPortHvac::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPortHvac::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPortHvac::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPortHvac::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<eevp::control::proxy::SoaHvacProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        SubscribeSoaHvacSwVersion();
        SubscribesoaHvacDeviceNormal();
        SubscribesoaHvacSetting();
        SubscribesoaHvacStatus();
        SubscribesoaHvacTemps();
    }
}
 
void RPortHvac::SubscribeSoaHvacSwVersion()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverSoaHvacSwVersion();
        
        // request subscribe
        auto subscribe = m_interface->SoaHvacSwVersion.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::SubscribeSoaHvacSwVersion::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortHvac::SubscribeSoaHvacSwVersion::" << subscribe.Error().Message();
        }
    }
}
 
void RPortHvac::StopSubscribeSoaHvacSwVersion()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->SoaHvacSwVersion.Unsubscribe();
        m_logger.LogVerbose() << "RPortHvac::StopSubscribeSoaHvacSwVersion::Unsubscribed";
    }
}
 
void RPortHvac::RegistReceiverSoaHvacSwVersion()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldSoaHvacSwVersionTriggered();
        };
        
        // regist callback
        auto callback = m_interface->SoaHvacSwVersion.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::RegistReceiverSoaHvacSwVersion::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortHvac::RegistReceiverSoaHvacSwVersion::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortHvac::ReceiveFieldSoaHvacSwVersionTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->SoaHvacSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->SoaHvacSwVersion.GetNewSamples([&](auto samplePtr) {
                RPortHvac::ReadValueSoaHvacSwVersion(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortHvac::ReceiveFieldSoaHvacSwVersion::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortHvac::ReceiveFieldSoaHvacSwVersion::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortHvac::ReceiveFieldSoaHvacSwVersionCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->SoaHvacSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->SoaHvacSwVersion.GetNewSamples([&](auto samplePtr) {
                    RPortHvac::ReadValueSoaHvacSwVersion(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortHvac::ReceiveFieldSoaHvacSwVersion::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortHvac::ReceiveFieldSoaHvacSwVersion::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortHvac::ReadValueSoaHvacSwVersion(ara::com::SamplePtr<eevp::control::proxy::fields::SoaHvacSwVersion::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiverSoaHvacSwVersion)
    {
        m_receiverSoaHvacSwVersion(value);
    }
}
 
void RPortHvac::RegistFieldHandlerSoaHvacSwVersion(std::function<void(const eevp::control::proxy::fields::SoaHvacSwVersion::FieldType&)> handler)
{
    m_receiverSoaHvacSwVersion = handler;
}
 
void RPortHvac::GetSoaHvacSwVersion()
{
    if (m_found)
    {
        auto request = m_interface->SoaHvacSwVersion.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::GetSoaHvacSwVersion::Responded";
            
            auto result = response.Value();
            if (m_receiverSoaHvacSwVersion)
            {
                m_receiverSoaHvacSwVersion(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortHvac::GetSoaHvacSwVersion::" << response.Error().Message();
        }
    }
}
 
void RPortHvac::SubscribesoaHvacDeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaHvacDeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->soaHvacDeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::SubscribesoaHvacDeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortHvac::SubscribesoaHvacDeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPortHvac::StopSubscribesoaHvacDeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaHvacDeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPortHvac::StopSubscribesoaHvacDeviceNormal::Unsubscribed";
    }
}
 
void RPortHvac::RegistReceiversoaHvacDeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaHvacDeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaHvacDeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::RegistReceiversoaHvacDeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortHvac::RegistReceiversoaHvacDeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortHvac::ReceiveFieldsoaHvacDeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaHvacDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacDeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPortHvac::ReadValuesoaHvacDeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortHvac::ReceiveFieldsoaHvacDeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortHvac::ReceiveFieldsoaHvacDeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortHvac::ReceiveFieldsoaHvacDeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaHvacDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacDeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPortHvac::ReadValuesoaHvacDeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortHvac::ReceiveFieldsoaHvacDeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortHvac::ReceiveFieldsoaHvacDeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortHvac::ReadValuesoaHvacDeviceNormal(ara::com::SamplePtr<eevp::control::proxy::fields::soaHvacDeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaHvacDeviceNormal)
    {
        m_receiversoaHvacDeviceNormal(value);
    }
}
 
void RPortHvac::RegistFieldHandlersoaHvacDeviceNormal(std::function<void(const eevp::control::proxy::fields::soaHvacDeviceNormal::FieldType&)> handler)
{
    m_receiversoaHvacDeviceNormal = handler;
}
 
void RPortHvac::GetsoaHvacDeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->soaHvacDeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::GetsoaHvacDeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiversoaHvacDeviceNormal)
            {
                m_receiversoaHvacDeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortHvac::GetsoaHvacDeviceNormal::" << response.Error().Message();
        }
    }
}
 
void RPortHvac::SubscribesoaHvacSetting()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaHvacSetting();
        
        // request subscribe
        auto subscribe = m_interface->soaHvacSetting.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::SubscribesoaHvacSetting::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortHvac::SubscribesoaHvacSetting::" << subscribe.Error().Message();
        }
    }
}
 
void RPortHvac::StopSubscribesoaHvacSetting()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaHvacSetting.Unsubscribe();
        m_logger.LogVerbose() << "RPortHvac::StopSubscribesoaHvacSetting::Unsubscribed";
    }
}
 
void RPortHvac::RegistReceiversoaHvacSetting()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaHvacSettingTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaHvacSetting.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::RegistReceiversoaHvacSetting::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortHvac::RegistReceiversoaHvacSetting::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortHvac::ReceiveFieldsoaHvacSettingTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaHvacSetting.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacSetting.GetNewSamples([&](auto samplePtr) {
                RPortHvac::ReadValuesoaHvacSetting(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortHvac::ReceiveFieldsoaHvacSetting::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortHvac::ReceiveFieldsoaHvacSetting::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortHvac::ReceiveFieldsoaHvacSettingCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaHvacSetting.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacSetting.GetNewSamples([&](auto samplePtr) {
                    RPortHvac::ReadValuesoaHvacSetting(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortHvac::ReceiveFieldsoaHvacSetting::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortHvac::ReceiveFieldsoaHvacSetting::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortHvac::ReadValuesoaHvacSetting(ara::com::SamplePtr<eevp::control::proxy::fields::soaHvacSetting::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaHvacSetting)
    {
        m_receiversoaHvacSetting(value);
    }
}
 
void RPortHvac::RegistFieldHandlersoaHvacSetting(std::function<void(const eevp::control::proxy::fields::soaHvacSetting::FieldType&)> handler)
{
    m_receiversoaHvacSetting = handler;
}
 
void RPortHvac::GetsoaHvacSetting()
{
    if (m_found)
    {
        auto request = m_interface->soaHvacSetting.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::GetsoaHvacSetting::Responded";
            
            auto result = response.Value();
            if (m_receiversoaHvacSetting)
            {
                m_receiversoaHvacSetting(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortHvac::GetsoaHvacSetting::" << response.Error().Message();
        }
    }
}
 
void RPortHvac::SubscribesoaHvacStatus()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaHvacStatus();
        
        // request subscribe
        auto subscribe = m_interface->soaHvacStatus.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::SubscribesoaHvacStatus::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortHvac::SubscribesoaHvacStatus::" << subscribe.Error().Message();
        }
    }
}
 
void RPortHvac::StopSubscribesoaHvacStatus()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaHvacStatus.Unsubscribe();
        m_logger.LogVerbose() << "RPortHvac::StopSubscribesoaHvacStatus::Unsubscribed";
    }
}
 
void RPortHvac::RegistReceiversoaHvacStatus()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaHvacStatusTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaHvacStatus.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::RegistReceiversoaHvacStatus::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortHvac::RegistReceiversoaHvacStatus::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortHvac::ReceiveFieldsoaHvacStatusTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaHvacStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacStatus.GetNewSamples([&](auto samplePtr) {
                RPortHvac::ReadValuesoaHvacStatus(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortHvac::ReceiveFieldsoaHvacStatus::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortHvac::ReceiveFieldsoaHvacStatus::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortHvac::ReceiveFieldsoaHvacStatusCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaHvacStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacStatus.GetNewSamples([&](auto samplePtr) {
                    RPortHvac::ReadValuesoaHvacStatus(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortHvac::ReceiveFieldsoaHvacStatus::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortHvac::ReceiveFieldsoaHvacStatus::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortHvac::ReadValuesoaHvacStatus(ara::com::SamplePtr<eevp::control::proxy::fields::soaHvacStatus::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaHvacStatus)
    {
        m_receiversoaHvacStatus(value);
    }
}
 
void RPortHvac::RegistFieldHandlersoaHvacStatus(std::function<void(const eevp::control::proxy::fields::soaHvacStatus::FieldType&)> handler)
{
    m_receiversoaHvacStatus = handler;
}
 
void RPortHvac::GetsoaHvacStatus()
{
    if (m_found)
    {
        auto request = m_interface->soaHvacStatus.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::GetsoaHvacStatus::Responded";
            
            auto result = response.Value();
            if (m_receiversoaHvacStatus)
            {
                m_receiversoaHvacStatus(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortHvac::GetsoaHvacStatus::" << response.Error().Message();
        }
    }
}
 
void RPortHvac::SubscribesoaHvacTemps()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaHvacTemps();
        
        // request subscribe
        auto subscribe = m_interface->soaHvacTemps.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::SubscribesoaHvacTemps::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortHvac::SubscribesoaHvacTemps::" << subscribe.Error().Message();
        }
    }
}
 
void RPortHvac::StopSubscribesoaHvacTemps()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaHvacTemps.Unsubscribe();
        m_logger.LogVerbose() << "RPortHvac::StopSubscribesoaHvacTemps::Unsubscribed";
    }
}
 
void RPortHvac::RegistReceiversoaHvacTemps()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaHvacTempsTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaHvacTemps.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::RegistReceiversoaHvacTemps::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortHvac::RegistReceiversoaHvacTemps::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortHvac::ReceiveFieldsoaHvacTempsTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaHvacTemps.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacTemps.GetNewSamples([&](auto samplePtr) {
                RPortHvac::ReadValuesoaHvacTemps(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortHvac::ReceiveFieldsoaHvacTemps::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortHvac::ReceiveFieldsoaHvacTemps::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortHvac::ReceiveFieldsoaHvacTempsCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaHvacTemps.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacTemps.GetNewSamples([&](auto samplePtr) {
                    RPortHvac::ReadValuesoaHvacTemps(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortHvac::ReceiveFieldsoaHvacTemps::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortHvac::ReceiveFieldsoaHvacTemps::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortHvac::ReadValuesoaHvacTemps(ara::com::SamplePtr<eevp::control::proxy::fields::soaHvacTemps::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaHvacTemps)
    {
        m_receiversoaHvacTemps(value);
    }
}
 
void RPortHvac::RegistFieldHandlersoaHvacTemps(std::function<void(const eevp::control::proxy::fields::soaHvacTemps::FieldType&)> handler)
{
    m_receiversoaHvacTemps = handler;
}
 
void RPortHvac::GetsoaHvacTemps()
{
    if (m_found)
    {
        auto request = m_interface->soaHvacTemps.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortHvac::GetsoaHvacTemps::Responded";
            
            auto result = response.Value();
            if (m_receiversoaHvacTemps)
            {
                m_receiversoaHvacTemps(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortHvac::GetsoaHvacTemps::" << response.Error().Message();
        }
    }
}
 
void RPortHvac::RequestSetAcOnOff(const bool& on)
{
    if (m_found)
    {
        m_interface->SetAcOnOff(on);
    }
}
 
void RPortHvac::RequestSetAirSource(const eevp::control::SoaHvacAirSource& src)
{
    if (m_found)
    {
        m_interface->SetAirSource(src);
    }
}
 
void RPortHvac::RequestSetAutoTemp(const bool& on)
{
    if (m_found)
    {
        m_interface->SetAutoTemp(on);
    }
}
 
void RPortHvac::RequestSetBackwardDefrostOn(const bool& on)
{
    if (m_found)
    {
        m_interface->SetBackwardDefrostOn(on);
    }
}
 
void RPortHvac::RequestSetBlowingDirection(const eevp::control::SoaHvacBlowDir& dir)
{
    if (m_found)
    {
        m_interface->SetBlowingDirection(dir);
    }
}
 
void RPortHvac::RequestSetBlowingForce(const std::uint8_t& force)
{
    if (m_found)
    {
        m_interface->SetBlowingForce(force);
    }
}
 
void RPortHvac::RequestSetDriverTemp(const std::int8_t& temperature)
{
    if (m_found)
    {
        m_interface->SetDriverTemp(temperature);
    }
}
 
void RPortHvac::RequestSetForwardDefrostOn(const bool& on)
{
    if (m_found)
    {
        m_interface->SetForwardDefrostOn(on);
    }
}
 
void RPortHvac::RequestSetHvacPower(const bool& on)
{
    if (m_found)
    {
        m_interface->SetHvacPower(on);
    }
}
 
void RPortHvac::RequestSetPassengerTemp(const std::int8_t& temperature)
{
    if (m_found)
    {
        m_interface->SetPassengerTemp(temperature);
    }
}
 
void RPortHvac::RequestSetSync(const bool& on)
{
    if (m_found)
    {
        m_interface->SetSync(on);
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace monitoringmanager
 
/// EOF