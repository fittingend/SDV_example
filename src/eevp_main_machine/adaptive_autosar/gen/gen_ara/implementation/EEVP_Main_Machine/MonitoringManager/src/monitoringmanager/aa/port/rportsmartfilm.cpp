///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rportsmartfilm.cpp
/// SOFTWARE COMPONENT NAME           : RPortSmartFilm
/// GENERATED DATE                    : 2025-09-05 11:31:33
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "monitoringmanager/aa/port/rportsmartfilm.h"
 
namespace monitoringmanager
{
namespace aa
{
namespace port
{
 
RPortSmartFilm::RPortSmartFilm()
    : m_logger(ara::log::CreateLogger("MONM", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiversoaFilmAuto{nullptr}
    , m_receiversoaFilmDeviceNormal{nullptr}
    , m_receiversoaFilmOpacities{nullptr}
    , m_receiversoaFilmSwVersion{nullptr}
{
}
 
RPortSmartFilm::~RPortSmartFilm()
{
}
 
void RPortSmartFilm::Start()
{
    m_logger.LogVerbose() << "RPortSmartFilm::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"MonitoringManager/AA/RPortSmartFilm"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::control::proxy::SoaSmartFilmProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::control::proxy::SoaSmartFilmProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPortSmartFilm::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPortSmartFilm::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPortSmartFilm::Terminate()
{
    m_logger.LogVerbose() << "RPortSmartFilm::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribesoaFilmAuto();
        StopSubscribesoaFilmDeviceNormal();
        StopSubscribesoaFilmOpacities();
        StopSubscribesoaFilmSwVersion();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPortSmartFilm::Terminate::StopFindService";
    }
}
 
void RPortSmartFilm::Find(ara::com::ServiceHandleContainer<eevp::control::proxy::SoaSmartFilmProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPortSmartFilm::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPortSmartFilm::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPortSmartFilm::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPortSmartFilm::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<eevp::control::proxy::SoaSmartFilmProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        SubscribesoaFilmAuto();
        SubscribesoaFilmDeviceNormal();
        SubscribesoaFilmOpacities();
        SubscribesoaFilmSwVersion();
    }
}
 
void RPortSmartFilm::SubscribesoaFilmAuto()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaFilmAuto();
        
        // request subscribe
        auto subscribe = m_interface->soaFilmAuto.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::SubscribesoaFilmAuto::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::SubscribesoaFilmAuto::" << subscribe.Error().Message();
        }
    }
}
 
void RPortSmartFilm::StopSubscribesoaFilmAuto()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaFilmAuto.Unsubscribe();
        m_logger.LogVerbose() << "RPortSmartFilm::StopSubscribesoaFilmAuto::Unsubscribed";
    }
}
 
void RPortSmartFilm::RegistReceiversoaFilmAuto()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaFilmAutoTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaFilmAuto.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::RegistReceiversoaFilmAuto::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::RegistReceiversoaFilmAuto::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortSmartFilm::ReceiveFieldsoaFilmAutoTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaFilmAuto.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaFilmAuto.GetNewSamples([&](auto samplePtr) {
                RPortSmartFilm::ReadValuesoaFilmAuto(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortSmartFilm::ReceiveFieldsoaFilmAuto::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortSmartFilm::ReceiveFieldsoaFilmAuto::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortSmartFilm::ReceiveFieldsoaFilmAutoCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaFilmAuto.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaFilmAuto.GetNewSamples([&](auto samplePtr) {
                    RPortSmartFilm::ReadValuesoaFilmAuto(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortSmartFilm::ReceiveFieldsoaFilmAuto::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortSmartFilm::ReceiveFieldsoaFilmAuto::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortSmartFilm::ReadValuesoaFilmAuto(ara::com::SamplePtr<eevp::control::proxy::fields::soaFilmAuto::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaFilmAuto)
    {
        m_receiversoaFilmAuto(value);
    }
}
 
void RPortSmartFilm::RegistFieldHandlersoaFilmAuto(std::function<void(const eevp::control::proxy::fields::soaFilmAuto::FieldType&)> handler)
{
    m_receiversoaFilmAuto = handler;
}
 
void RPortSmartFilm::GetsoaFilmAuto()
{
    if (m_found)
    {
        auto request = m_interface->soaFilmAuto.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::GetsoaFilmAuto::Responded";
            
            auto result = response.Value();
            if (m_receiversoaFilmAuto)
            {
                m_receiversoaFilmAuto(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::GetsoaFilmAuto::" << response.Error().Message();
        }
    }
}
 
void RPortSmartFilm::SubscribesoaFilmDeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaFilmDeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->soaFilmDeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::SubscribesoaFilmDeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::SubscribesoaFilmDeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPortSmartFilm::StopSubscribesoaFilmDeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaFilmDeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPortSmartFilm::StopSubscribesoaFilmDeviceNormal::Unsubscribed";
    }
}
 
void RPortSmartFilm::RegistReceiversoaFilmDeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaFilmDeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaFilmDeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::RegistReceiversoaFilmDeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::RegistReceiversoaFilmDeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortSmartFilm::ReceiveFieldsoaFilmDeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaFilmDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaFilmDeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPortSmartFilm::ReadValuesoaFilmDeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortSmartFilm::ReceiveFieldsoaFilmDeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortSmartFilm::ReceiveFieldsoaFilmDeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortSmartFilm::ReceiveFieldsoaFilmDeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaFilmDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaFilmDeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPortSmartFilm::ReadValuesoaFilmDeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortSmartFilm::ReceiveFieldsoaFilmDeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortSmartFilm::ReceiveFieldsoaFilmDeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortSmartFilm::ReadValuesoaFilmDeviceNormal(ara::com::SamplePtr<eevp::control::proxy::fields::soaFilmDeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaFilmDeviceNormal)
    {
        m_receiversoaFilmDeviceNormal(value);
    }
}
 
void RPortSmartFilm::RegistFieldHandlersoaFilmDeviceNormal(std::function<void(const eevp::control::proxy::fields::soaFilmDeviceNormal::FieldType&)> handler)
{
    m_receiversoaFilmDeviceNormal = handler;
}
 
void RPortSmartFilm::GetsoaFilmDeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->soaFilmDeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::GetsoaFilmDeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiversoaFilmDeviceNormal)
            {
                m_receiversoaFilmDeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::GetsoaFilmDeviceNormal::" << response.Error().Message();
        }
    }
}
 
void RPortSmartFilm::SubscribesoaFilmOpacities()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaFilmOpacities();
        
        // request subscribe
        auto subscribe = m_interface->soaFilmOpacities.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::SubscribesoaFilmOpacities::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::SubscribesoaFilmOpacities::" << subscribe.Error().Message();
        }
    }
}
 
void RPortSmartFilm::StopSubscribesoaFilmOpacities()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaFilmOpacities.Unsubscribe();
        m_logger.LogVerbose() << "RPortSmartFilm::StopSubscribesoaFilmOpacities::Unsubscribed";
    }
}
 
void RPortSmartFilm::RegistReceiversoaFilmOpacities()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaFilmOpacitiesTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaFilmOpacities.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::RegistReceiversoaFilmOpacities::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::RegistReceiversoaFilmOpacities::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortSmartFilm::ReceiveFieldsoaFilmOpacitiesTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaFilmOpacities.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaFilmOpacities.GetNewSamples([&](auto samplePtr) {
                RPortSmartFilm::ReadValuesoaFilmOpacities(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortSmartFilm::ReceiveFieldsoaFilmOpacities::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortSmartFilm::ReceiveFieldsoaFilmOpacities::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortSmartFilm::ReceiveFieldsoaFilmOpacitiesCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaFilmOpacities.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaFilmOpacities.GetNewSamples([&](auto samplePtr) {
                    RPortSmartFilm::ReadValuesoaFilmOpacities(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortSmartFilm::ReceiveFieldsoaFilmOpacities::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortSmartFilm::ReceiveFieldsoaFilmOpacities::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortSmartFilm::ReadValuesoaFilmOpacities(ara::com::SamplePtr<eevp::control::proxy::fields::soaFilmOpacities::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaFilmOpacities)
    {
        m_receiversoaFilmOpacities(value);
    }
}
 
void RPortSmartFilm::RegistFieldHandlersoaFilmOpacities(std::function<void(const eevp::control::proxy::fields::soaFilmOpacities::FieldType&)> handler)
{
    m_receiversoaFilmOpacities = handler;
}
 
void RPortSmartFilm::GetsoaFilmOpacities()
{
    if (m_found)
    {
        auto request = m_interface->soaFilmOpacities.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::GetsoaFilmOpacities::Responded";
            
            auto result = response.Value();
            if (m_receiversoaFilmOpacities)
            {
                m_receiversoaFilmOpacities(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::GetsoaFilmOpacities::" << response.Error().Message();
        }
    }
}
 
void RPortSmartFilm::SubscribesoaFilmSwVersion()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaFilmSwVersion();
        
        // request subscribe
        auto subscribe = m_interface->soaFilmSwVersion.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::SubscribesoaFilmSwVersion::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::SubscribesoaFilmSwVersion::" << subscribe.Error().Message();
        }
    }
}
 
void RPortSmartFilm::StopSubscribesoaFilmSwVersion()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaFilmSwVersion.Unsubscribe();
        m_logger.LogVerbose() << "RPortSmartFilm::StopSubscribesoaFilmSwVersion::Unsubscribed";
    }
}
 
void RPortSmartFilm::RegistReceiversoaFilmSwVersion()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaFilmSwVersionTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaFilmSwVersion.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::RegistReceiversoaFilmSwVersion::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::RegistReceiversoaFilmSwVersion::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortSmartFilm::ReceiveFieldsoaFilmSwVersionTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaFilmSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaFilmSwVersion.GetNewSamples([&](auto samplePtr) {
                RPortSmartFilm::ReadValuesoaFilmSwVersion(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortSmartFilm::ReceiveFieldsoaFilmSwVersion::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortSmartFilm::ReceiveFieldsoaFilmSwVersion::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortSmartFilm::ReceiveFieldsoaFilmSwVersionCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaFilmSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaFilmSwVersion.GetNewSamples([&](auto samplePtr) {
                    RPortSmartFilm::ReadValuesoaFilmSwVersion(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortSmartFilm::ReceiveFieldsoaFilmSwVersion::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortSmartFilm::ReceiveFieldsoaFilmSwVersion::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortSmartFilm::ReadValuesoaFilmSwVersion(ara::com::SamplePtr<eevp::control::proxy::fields::soaFilmSwVersion::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaFilmSwVersion)
    {
        m_receiversoaFilmSwVersion(value);
    }
}
 
void RPortSmartFilm::RegistFieldHandlersoaFilmSwVersion(std::function<void(const eevp::control::proxy::fields::soaFilmSwVersion::FieldType&)> handler)
{
    m_receiversoaFilmSwVersion = handler;
}
 
void RPortSmartFilm::GetsoaFilmSwVersion()
{
    if (m_found)
    {
        auto request = m_interface->soaFilmSwVersion.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortSmartFilm::GetsoaFilmSwVersion::Responded";
            
            auto result = response.Value();
            if (m_receiversoaFilmSwVersion)
            {
                m_receiversoaFilmSwVersion(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortSmartFilm::GetsoaFilmSwVersion::" << response.Error().Message();
        }
    }
}
 
void RPortSmartFilm::RequestSetAllOpacities(const std::uint8_t& opacity)
{
    if (m_found)
    {
        m_interface->SetAllOpacities(opacity);
    }
}
 
void RPortSmartFilm::RequestSetAuto(const bool& on)
{
    if (m_found)
    {
        m_interface->SetAuto(on);
    }
}
 
void RPortSmartFilm::RequestSetOpacity(const eevp::control::SoaFilmPos& pos, const std::uint8_t& opacity)
{
    if (m_found)
    {
        m_interface->SetOpacity(pos, opacity);
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace monitoringmanager
 
/// EOF