///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rportdrvseat.cpp
/// SOFTWARE COMPONENT NAME           : RPortDrvSeat
/// GENERATED DATE                    : 2025-09-05 11:31:33
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "monitoringmanager/aa/port/rportdrvseat.h"
 
namespace monitoringmanager
{
namespace aa
{
namespace port
{
 
RPortDrvSeat::RPortDrvSeat()
    : m_logger(ara::log::CreateLogger("MONM", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiversoaDrvSeatDeviceNormal{nullptr}
    , m_receiversoaDrvSeatMotorDirection{nullptr}
    , m_receiversoaDrvSeatMotorPosition{nullptr}
    , m_receiversoaDrvSeatSwVersion{nullptr}
{
}
 
RPortDrvSeat::~RPortDrvSeat()
{
}
 
void RPortDrvSeat::Start()
{
    m_logger.LogVerbose() << "RPortDrvSeat::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"MonitoringManager/AA/RPortDrvSeat"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::control::proxy::SoaDriverSeatProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::control::proxy::SoaDriverSeatProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPortDrvSeat::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPortDrvSeat::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPortDrvSeat::Terminate()
{
    m_logger.LogVerbose() << "RPortDrvSeat::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribesoaDrvSeatDeviceNormal();
        StopSubscribesoaDrvSeatMotorDirection();
        StopSubscribesoaDrvSeatMotorPosition();
        StopSubscribesoaDrvSeatSwVersion();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPortDrvSeat::Terminate::StopFindService";
    }
}
 
void RPortDrvSeat::Find(ara::com::ServiceHandleContainer<eevp::control::proxy::SoaDriverSeatProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPortDrvSeat::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPortDrvSeat::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPortDrvSeat::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPortDrvSeat::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<eevp::control::proxy::SoaDriverSeatProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        SubscribesoaDrvSeatDeviceNormal();
        SubscribesoaDrvSeatMotorDirection();
        SubscribesoaDrvSeatMotorPosition();
        SubscribesoaDrvSeatSwVersion();
    }
}
 
void RPortDrvSeat::SubscribesoaDrvSeatDeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaDrvSeatDeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->soaDrvSeatDeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::SubscribesoaDrvSeatDeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::SubscribesoaDrvSeatDeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPortDrvSeat::StopSubscribesoaDrvSeatDeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaDrvSeatDeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPortDrvSeat::StopSubscribesoaDrvSeatDeviceNormal::Unsubscribed";
    }
}
 
void RPortDrvSeat::RegistReceiversoaDrvSeatDeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaDrvSeatDeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaDrvSeatDeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::RegistReceiversoaDrvSeatDeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::RegistReceiversoaDrvSeatDeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortDrvSeat::ReceiveFieldsoaDrvSeatDeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaDrvSeatDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDrvSeatDeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPortDrvSeat::ReadValuesoaDrvSeatDeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatDeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatDeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortDrvSeat::ReceiveFieldsoaDrvSeatDeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaDrvSeatDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDrvSeatDeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPortDrvSeat::ReadValuesoaDrvSeatDeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatDeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatDeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortDrvSeat::ReadValuesoaDrvSeatDeviceNormal(ara::com::SamplePtr<eevp::control::proxy::fields::soaDrvSeatDeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaDrvSeatDeviceNormal)
    {
        m_receiversoaDrvSeatDeviceNormal(value);
    }
}
 
void RPortDrvSeat::RegistFieldHandlersoaDrvSeatDeviceNormal(std::function<void(const eevp::control::proxy::fields::soaDrvSeatDeviceNormal::FieldType&)> handler)
{
    m_receiversoaDrvSeatDeviceNormal = handler;
}
 
void RPortDrvSeat::GetsoaDrvSeatDeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->soaDrvSeatDeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::GetsoaDrvSeatDeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiversoaDrvSeatDeviceNormal)
            {
                m_receiversoaDrvSeatDeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::GetsoaDrvSeatDeviceNormal::" << response.Error().Message();
        }
    }
}
 
void RPortDrvSeat::SubscribesoaDrvSeatMotorDirection()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaDrvSeatMotorDirection();
        
        // request subscribe
        auto subscribe = m_interface->soaDrvSeatMotorDirection.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::SubscribesoaDrvSeatMotorDirection::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::SubscribesoaDrvSeatMotorDirection::" << subscribe.Error().Message();
        }
    }
}
 
void RPortDrvSeat::StopSubscribesoaDrvSeatMotorDirection()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaDrvSeatMotorDirection.Unsubscribe();
        m_logger.LogVerbose() << "RPortDrvSeat::StopSubscribesoaDrvSeatMotorDirection::Unsubscribed";
    }
}
 
void RPortDrvSeat::RegistReceiversoaDrvSeatMotorDirection()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaDrvSeatMotorDirectionTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaDrvSeatMotorDirection.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::RegistReceiversoaDrvSeatMotorDirection::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::RegistReceiversoaDrvSeatMotorDirection::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorDirectionTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaDrvSeatMotorDirection.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDrvSeatMotorDirection.GetNewSamples([&](auto samplePtr) {
                RPortDrvSeat::ReadValuesoaDrvSeatMotorDirection(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorDirection::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorDirection::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorDirectionCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaDrvSeatMotorDirection.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDrvSeatMotorDirection.GetNewSamples([&](auto samplePtr) {
                    RPortDrvSeat::ReadValuesoaDrvSeatMotorDirection(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorDirection::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorDirection::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortDrvSeat::ReadValuesoaDrvSeatMotorDirection(ara::com::SamplePtr<eevp::control::proxy::fields::soaDrvSeatMotorDirection::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaDrvSeatMotorDirection)
    {
        m_receiversoaDrvSeatMotorDirection(value);
    }
}
 
void RPortDrvSeat::RegistFieldHandlersoaDrvSeatMotorDirection(std::function<void(const eevp::control::proxy::fields::soaDrvSeatMotorDirection::FieldType&)> handler)
{
    m_receiversoaDrvSeatMotorDirection = handler;
}
 
void RPortDrvSeat::GetsoaDrvSeatMotorDirection()
{
    if (m_found)
    {
        auto request = m_interface->soaDrvSeatMotorDirection.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::GetsoaDrvSeatMotorDirection::Responded";
            
            auto result = response.Value();
            if (m_receiversoaDrvSeatMotorDirection)
            {
                m_receiversoaDrvSeatMotorDirection(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::GetsoaDrvSeatMotorDirection::" << response.Error().Message();
        }
    }
}
 
void RPortDrvSeat::SubscribesoaDrvSeatMotorPosition()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaDrvSeatMotorPosition();
        
        // request subscribe
        auto subscribe = m_interface->soaDrvSeatMotorPosition.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::SubscribesoaDrvSeatMotorPosition::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::SubscribesoaDrvSeatMotorPosition::" << subscribe.Error().Message();
        }
    }
}
 
void RPortDrvSeat::StopSubscribesoaDrvSeatMotorPosition()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaDrvSeatMotorPosition.Unsubscribe();
        m_logger.LogVerbose() << "RPortDrvSeat::StopSubscribesoaDrvSeatMotorPosition::Unsubscribed";
    }
}
 
void RPortDrvSeat::RegistReceiversoaDrvSeatMotorPosition()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaDrvSeatMotorPositionTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaDrvSeatMotorPosition.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::RegistReceiversoaDrvSeatMotorPosition::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::RegistReceiversoaDrvSeatMotorPosition::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorPositionTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaDrvSeatMotorPosition.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDrvSeatMotorPosition.GetNewSamples([&](auto samplePtr) {
                RPortDrvSeat::ReadValuesoaDrvSeatMotorPosition(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorPosition::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorPosition::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorPositionCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaDrvSeatMotorPosition.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDrvSeatMotorPosition.GetNewSamples([&](auto samplePtr) {
                    RPortDrvSeat::ReadValuesoaDrvSeatMotorPosition(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorPosition::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatMotorPosition::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortDrvSeat::ReadValuesoaDrvSeatMotorPosition(ara::com::SamplePtr<eevp::control::proxy::fields::soaDrvSeatMotorPosition::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaDrvSeatMotorPosition)
    {
        m_receiversoaDrvSeatMotorPosition(value);
    }
}
 
void RPortDrvSeat::RegistFieldHandlersoaDrvSeatMotorPosition(std::function<void(const eevp::control::proxy::fields::soaDrvSeatMotorPosition::FieldType&)> handler)
{
    m_receiversoaDrvSeatMotorPosition = handler;
}
 
void RPortDrvSeat::GetsoaDrvSeatMotorPosition()
{
    if (m_found)
    {
        auto request = m_interface->soaDrvSeatMotorPosition.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::GetsoaDrvSeatMotorPosition::Responded";
            
            auto result = response.Value();
            if (m_receiversoaDrvSeatMotorPosition)
            {
                m_receiversoaDrvSeatMotorPosition(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::GetsoaDrvSeatMotorPosition::" << response.Error().Message();
        }
    }
}
 
void RPortDrvSeat::SubscribesoaDrvSeatSwVersion()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaDrvSeatSwVersion();
        
        // request subscribe
        auto subscribe = m_interface->soaDrvSeatSwVersion.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::SubscribesoaDrvSeatSwVersion::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::SubscribesoaDrvSeatSwVersion::" << subscribe.Error().Message();
        }
    }
}
 
void RPortDrvSeat::StopSubscribesoaDrvSeatSwVersion()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaDrvSeatSwVersion.Unsubscribe();
        m_logger.LogVerbose() << "RPortDrvSeat::StopSubscribesoaDrvSeatSwVersion::Unsubscribed";
    }
}
 
void RPortDrvSeat::RegistReceiversoaDrvSeatSwVersion()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaDrvSeatSwVersionTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaDrvSeatSwVersion.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::RegistReceiversoaDrvSeatSwVersion::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::RegistReceiversoaDrvSeatSwVersion::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortDrvSeat::ReceiveFieldsoaDrvSeatSwVersionTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaDrvSeatSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDrvSeatSwVersion.GetNewSamples([&](auto samplePtr) {
                RPortDrvSeat::ReadValuesoaDrvSeatSwVersion(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatSwVersion::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatSwVersion::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortDrvSeat::ReceiveFieldsoaDrvSeatSwVersionCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaDrvSeatSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDrvSeatSwVersion.GetNewSamples([&](auto samplePtr) {
                    RPortDrvSeat::ReadValuesoaDrvSeatSwVersion(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatSwVersion::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortDrvSeat::ReceiveFieldsoaDrvSeatSwVersion::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortDrvSeat::ReadValuesoaDrvSeatSwVersion(ara::com::SamplePtr<eevp::control::proxy::fields::soaDrvSeatSwVersion::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaDrvSeatSwVersion)
    {
        m_receiversoaDrvSeatSwVersion(value);
    }
}
 
void RPortDrvSeat::RegistFieldHandlersoaDrvSeatSwVersion(std::function<void(const eevp::control::proxy::fields::soaDrvSeatSwVersion::FieldType&)> handler)
{
    m_receiversoaDrvSeatSwVersion = handler;
}
 
void RPortDrvSeat::GetsoaDrvSeatSwVersion()
{
    if (m_found)
    {
        auto request = m_interface->soaDrvSeatSwVersion.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortDrvSeat::GetsoaDrvSeatSwVersion::Responded";
            
            auto result = response.Value();
            if (m_receiversoaDrvSeatSwVersion)
            {
                m_receiversoaDrvSeatSwVersion(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortDrvSeat::GetsoaDrvSeatSwVersion::" << response.Error().Message();
        }
    }
}
 
void RPortDrvSeat::RequestRequestMoveDrvSeatHeight(const eevp::control::SoaSeatHeightDirection& dir)
{
    if (m_found)
    {
        m_interface->RequestMoveDrvSeatHeight(dir);
    }
}
 
void RPortDrvSeat::RequestRequestMoveDrvSeatRecline(const eevp::control::SoaSeatReclineDirection& dir)
{
    if (m_found)
    {
        m_interface->RequestMoveDrvSeatRecline(dir);
    }
}
 
void RPortDrvSeat::RequestRequestMoveDrvSeatSlide(const eevp::control::SoaSeatSlideDirection& dir)
{
    if (m_found)
    {
        m_interface->RequestMoveDrvSeatSlide(dir);
    }
}
 
void RPortDrvSeat::RequestRequestMoveDrvSeatTilt(const eevp::control::SoaSeatTiltDirection& dir)
{
    if (m_found)
    {
        m_interface->RequestMoveDrvSeatTilt(dir);
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace monitoringmanager
 
/// EOF