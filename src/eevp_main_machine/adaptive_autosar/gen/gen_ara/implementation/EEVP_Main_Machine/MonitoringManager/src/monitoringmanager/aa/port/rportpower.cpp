///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rportpower.cpp
/// SOFTWARE COMPONENT NAME           : RPortPower
/// GENERATED DATE                    : 2025-09-05 11:31:33
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "monitoringmanager/aa/port/rportpower.h"
 
namespace monitoringmanager
{
namespace aa
{
namespace port
{
 
RPortPower::RPortPower()
    : m_logger(ara::log::CreateLogger("MONM", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiversoaPowerDeviceNormal{nullptr}
    , m_receiversoaPowerSwVersion{nullptr}
{
}
 
RPortPower::~RPortPower()
{
}
 
void RPortPower::Start()
{
    m_logger.LogVerbose() << "RPortPower::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"MonitoringManager/AA/RPortPower"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::control::proxy::SoaPowerProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::control::proxy::SoaPowerProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPortPower::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPortPower::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPortPower::Terminate()
{
    m_logger.LogVerbose() << "RPortPower::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribesoaPowerDeviceNormal();
        StopSubscribesoaPowerSwVersion();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPortPower::Terminate::StopFindService";
    }
}
 
void RPortPower::Find(ara::com::ServiceHandleContainer<eevp::control::proxy::SoaPowerProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPortPower::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPortPower::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPortPower::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPortPower::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<eevp::control::proxy::SoaPowerProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        SubscribesoaPowerDeviceNormal();
        SubscribesoaPowerSwVersion();
    }
}
 
void RPortPower::SubscribesoaPowerDeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaPowerDeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->soaPowerDeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortPower::SubscribesoaPowerDeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortPower::SubscribesoaPowerDeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPortPower::StopSubscribesoaPowerDeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaPowerDeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPortPower::StopSubscribesoaPowerDeviceNormal::Unsubscribed";
    }
}
 
void RPortPower::RegistReceiversoaPowerDeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaPowerDeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaPowerDeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortPower::RegistReceiversoaPowerDeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortPower::RegistReceiversoaPowerDeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortPower::ReceiveFieldsoaPowerDeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaPowerDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaPowerDeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPortPower::ReadValuesoaPowerDeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortPower::ReceiveFieldsoaPowerDeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortPower::ReceiveFieldsoaPowerDeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortPower::ReceiveFieldsoaPowerDeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaPowerDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaPowerDeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPortPower::ReadValuesoaPowerDeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortPower::ReceiveFieldsoaPowerDeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortPower::ReceiveFieldsoaPowerDeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortPower::ReadValuesoaPowerDeviceNormal(ara::com::SamplePtr<eevp::control::proxy::fields::soaPowerDeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaPowerDeviceNormal)
    {
        m_receiversoaPowerDeviceNormal(value);
    }
}
 
void RPortPower::RegistFieldHandlersoaPowerDeviceNormal(std::function<void(const eevp::control::proxy::fields::soaPowerDeviceNormal::FieldType&)> handler)
{
    m_receiversoaPowerDeviceNormal = handler;
}
 
void RPortPower::GetsoaPowerDeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->soaPowerDeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortPower::GetsoaPowerDeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiversoaPowerDeviceNormal)
            {
                m_receiversoaPowerDeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortPower::GetsoaPowerDeviceNormal::" << response.Error().Message();
        }
    }
}
 
void RPortPower::SubscribesoaPowerSwVersion()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaPowerSwVersion();
        
        // request subscribe
        auto subscribe = m_interface->soaPowerSwVersion.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortPower::SubscribesoaPowerSwVersion::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortPower::SubscribesoaPowerSwVersion::" << subscribe.Error().Message();
        }
    }
}
 
void RPortPower::StopSubscribesoaPowerSwVersion()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaPowerSwVersion.Unsubscribe();
        m_logger.LogVerbose() << "RPortPower::StopSubscribesoaPowerSwVersion::Unsubscribed";
    }
}
 
void RPortPower::RegistReceiversoaPowerSwVersion()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaPowerSwVersionTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaPowerSwVersion.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortPower::RegistReceiversoaPowerSwVersion::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortPower::RegistReceiversoaPowerSwVersion::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortPower::ReceiveFieldsoaPowerSwVersionTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaPowerSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaPowerSwVersion.GetNewSamples([&](auto samplePtr) {
                RPortPower::ReadValuesoaPowerSwVersion(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortPower::ReceiveFieldsoaPowerSwVersion::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortPower::ReceiveFieldsoaPowerSwVersion::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortPower::ReceiveFieldsoaPowerSwVersionCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaPowerSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaPowerSwVersion.GetNewSamples([&](auto samplePtr) {
                    RPortPower::ReadValuesoaPowerSwVersion(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortPower::ReceiveFieldsoaPowerSwVersion::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortPower::ReceiveFieldsoaPowerSwVersion::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortPower::ReadValuesoaPowerSwVersion(ara::com::SamplePtr<eevp::control::proxy::fields::soaPowerSwVersion::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaPowerSwVersion)
    {
        m_receiversoaPowerSwVersion(value);
    }
}
 
void RPortPower::RegistFieldHandlersoaPowerSwVersion(std::function<void(const eevp::control::proxy::fields::soaPowerSwVersion::FieldType&)> handler)
{
    m_receiversoaPowerSwVersion = handler;
}
 
void RPortPower::GetsoaPowerSwVersion()
{
    if (m_found)
    {
        auto request = m_interface->soaPowerSwVersion.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortPower::GetsoaPowerSwVersion::Responded";
            
            auto result = response.Value();
            if (m_receiversoaPowerSwVersion)
            {
                m_receiversoaPowerSwVersion(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortPower::GetsoaPowerSwVersion::" << response.Error().Message();
        }
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace monitoringmanager
 
/// EOF