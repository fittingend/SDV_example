///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rportdms.cpp
/// SOFTWARE COMPONENT NAME           : RPortDms
/// GENERATED DATE                    : 2025-09-05 11:31:33
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "monitoringmanager/aa/port/rportdms.h"
 
namespace monitoringmanager
{
namespace aa
{
namespace port
{
 
RPortDms::RPortDms()
    : m_logger(ara::log::CreateLogger("MONM", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiveronDriverNotFocusing{nullptr}
    , m_receiveronDriverNotWaken{nullptr}
    , m_receiveronDriverNotWatchingFront{nullptr}
    , m_receiversoaDmsDeviceNormal{nullptr}
    , m_receiversoaDmsDriverStatus{nullptr}
    , m_receiversoaDmsStatus{nullptr}
    , m_receiversoaDmsSwVersion{nullptr}
{
}
 
RPortDms::~RPortDms()
{
}
 
void RPortDms::Start()
{
    m_logger.LogVerbose() << "RPortDms::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"MonitoringManager/AA/RPortDms"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::control::proxy::SoaDmsProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::control::proxy::SoaDmsProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPortDms::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPortDms::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPortDms::Terminate()
{
    m_logger.LogVerbose() << "RPortDms::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        // stop subscribe
        StopSubscribeonDriverNotFocusing();
        StopSubscribeonDriverNotWaken();
        StopSubscribeonDriverNotWatchingFront();
        StopSubscribesoaDmsDeviceNormal();
        StopSubscribesoaDmsDriverStatus();
        StopSubscribesoaDmsStatus();
        StopSubscribesoaDmsSwVersion();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPortDms::Terminate::StopFindService";
    }
}
 
void RPortDms::Find(ara::com::ServiceHandleContainer<eevp::control::proxy::SoaDmsProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPortDms::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPortDms::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPortDms::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPortDms::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<eevp::control::proxy::SoaDmsProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe events
        SubscribeonDriverNotFocusing();
        SubscribeonDriverNotWaken();
        SubscribeonDriverNotWatchingFront();
        // subscribe field notifications
        SubscribesoaDmsDeviceNormal();
        SubscribesoaDmsDriverStatus();
        SubscribesoaDmsStatus();
        SubscribesoaDmsSwVersion();
    }
}
 
void RPortDms::SubscribeonDriverNotFocusing()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiveronDriverNotFocusing();
        
        // request subscribe
        auto subscribe = m_interface->onDriverNotFocusing.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::SubscribeonDriverNotFocusing::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortDms::SubscribeonDriverNotFocusing::" << subscribe.Error().Message();
        }
    }
}
 
void RPortDms::StopSubscribeonDriverNotFocusing()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->onDriverNotFocusing.Unsubscribe();
        m_logger.LogVerbose() << "RPortDms::StopSubscribeonDriverNotFocusing::Unsubscribed";
    }
}
 
void RPortDms::RegistReceiveronDriverNotFocusing()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventonDriverNotFocusingTriggered();
        };
        
        // regist callback
        auto callback = m_interface->onDriverNotFocusing.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::RegistReceiveronDriverNotFocusing::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortDms::RegistReceiveronDriverNotFocusing::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortDms::ReceiveEventonDriverNotFocusingTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->onDriverNotFocusing.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->onDriverNotFocusing.GetNewSamples([&](auto samplePtr) {
                RPortDms::ReadDataonDriverNotFocusing(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortDms::ReceiveEventonDriverNotFocusing::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortDms::ReceiveEventonDriverNotFocusing::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortDms::ReceiveEventonDriverNotFocusingCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->onDriverNotFocusing.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->onDriverNotFocusing.GetNewSamples([&](auto samplePtr) {
                    RPortDms::ReadDataonDriverNotFocusing(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortDms::ReceiveEventonDriverNotFocusing::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortDms::ReceiveEventonDriverNotFocusing::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortDms::ReadDataonDriverNotFocusing(ara::com::SamplePtr<eevp::control::proxy::events::onDriverNotFocusing::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiveronDriverNotFocusing)
    {
        m_receiveronDriverNotFocusing(data);
    }
}
 
void RPortDms::RegistEventHandleronDriverNotFocusing(std::function<void(const eevp::control::proxy::events::onDriverNotFocusing::SampleType&)> handler)
{
    m_receiveronDriverNotFocusing = handler;
}
 
void RPortDms::SubscribeonDriverNotWaken()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiveronDriverNotWaken();
        
        // request subscribe
        auto subscribe = m_interface->onDriverNotWaken.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::SubscribeonDriverNotWaken::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortDms::SubscribeonDriverNotWaken::" << subscribe.Error().Message();
        }
    }
}
 
void RPortDms::StopSubscribeonDriverNotWaken()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->onDriverNotWaken.Unsubscribe();
        m_logger.LogVerbose() << "RPortDms::StopSubscribeonDriverNotWaken::Unsubscribed";
    }
}
 
void RPortDms::RegistReceiveronDriverNotWaken()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventonDriverNotWakenTriggered();
        };
        
        // regist callback
        auto callback = m_interface->onDriverNotWaken.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::RegistReceiveronDriverNotWaken::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortDms::RegistReceiveronDriverNotWaken::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortDms::ReceiveEventonDriverNotWakenTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->onDriverNotWaken.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->onDriverNotWaken.GetNewSamples([&](auto samplePtr) {
                RPortDms::ReadDataonDriverNotWaken(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortDms::ReceiveEventonDriverNotWaken::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortDms::ReceiveEventonDriverNotWaken::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortDms::ReceiveEventonDriverNotWakenCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->onDriverNotWaken.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->onDriverNotWaken.GetNewSamples([&](auto samplePtr) {
                    RPortDms::ReadDataonDriverNotWaken(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortDms::ReceiveEventonDriverNotWaken::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortDms::ReceiveEventonDriverNotWaken::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortDms::ReadDataonDriverNotWaken(ara::com::SamplePtr<eevp::control::proxy::events::onDriverNotWaken::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiveronDriverNotWaken)
    {
        m_receiveronDriverNotWaken(data);
    }
}
 
void RPortDms::RegistEventHandleronDriverNotWaken(std::function<void(const eevp::control::proxy::events::onDriverNotWaken::SampleType&)> handler)
{
    m_receiveronDriverNotWaken = handler;
}
 
void RPortDms::SubscribeonDriverNotWatchingFront()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiveronDriverNotWatchingFront();
        
        // request subscribe
        auto subscribe = m_interface->onDriverNotWatchingFront.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::SubscribeonDriverNotWatchingFront::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortDms::SubscribeonDriverNotWatchingFront::" << subscribe.Error().Message();
        }
    }
}
 
void RPortDms::StopSubscribeonDriverNotWatchingFront()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->onDriverNotWatchingFront.Unsubscribe();
        m_logger.LogVerbose() << "RPortDms::StopSubscribeonDriverNotWatchingFront::Unsubscribed";
    }
}
 
void RPortDms::RegistReceiveronDriverNotWatchingFront()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventonDriverNotWatchingFrontTriggered();
        };
        
        // regist callback
        auto callback = m_interface->onDriverNotWatchingFront.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::RegistReceiveronDriverNotWatchingFront::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortDms::RegistReceiveronDriverNotWatchingFront::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortDms::ReceiveEventonDriverNotWatchingFrontTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->onDriverNotWatchingFront.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->onDriverNotWatchingFront.GetNewSamples([&](auto samplePtr) {
                RPortDms::ReadDataonDriverNotWatchingFront(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortDms::ReceiveEventonDriverNotWatchingFront::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortDms::ReceiveEventonDriverNotWatchingFront::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortDms::ReceiveEventonDriverNotWatchingFrontCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->onDriverNotWatchingFront.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->onDriverNotWatchingFront.GetNewSamples([&](auto samplePtr) {
                    RPortDms::ReadDataonDriverNotWatchingFront(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortDms::ReceiveEventonDriverNotWatchingFront::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortDms::ReceiveEventonDriverNotWatchingFront::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortDms::ReadDataonDriverNotWatchingFront(ara::com::SamplePtr<eevp::control::proxy::events::onDriverNotWatchingFront::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiveronDriverNotWatchingFront)
    {
        m_receiveronDriverNotWatchingFront(data);
    }
}
 
void RPortDms::RegistEventHandleronDriverNotWatchingFront(std::function<void(const eevp::control::proxy::events::onDriverNotWatchingFront::SampleType&)> handler)
{
    m_receiveronDriverNotWatchingFront = handler;
}
 
void RPortDms::SubscribesoaDmsDeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaDmsDeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->soaDmsDeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::SubscribesoaDmsDeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortDms::SubscribesoaDmsDeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPortDms::StopSubscribesoaDmsDeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaDmsDeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPortDms::StopSubscribesoaDmsDeviceNormal::Unsubscribed";
    }
}
 
void RPortDms::RegistReceiversoaDmsDeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaDmsDeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaDmsDeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::RegistReceiversoaDmsDeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortDms::RegistReceiversoaDmsDeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortDms::ReceiveFieldsoaDmsDeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaDmsDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDmsDeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPortDms::ReadValuesoaDmsDeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortDms::ReceiveFieldsoaDmsDeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortDms::ReceiveFieldsoaDmsDeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortDms::ReceiveFieldsoaDmsDeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaDmsDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDmsDeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPortDms::ReadValuesoaDmsDeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortDms::ReceiveFieldsoaDmsDeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortDms::ReceiveFieldsoaDmsDeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortDms::ReadValuesoaDmsDeviceNormal(ara::com::SamplePtr<eevp::control::proxy::fields::soaDmsDeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaDmsDeviceNormal)
    {
        m_receiversoaDmsDeviceNormal(value);
    }
}
 
void RPortDms::RegistFieldHandlersoaDmsDeviceNormal(std::function<void(const eevp::control::proxy::fields::soaDmsDeviceNormal::FieldType&)> handler)
{
    m_receiversoaDmsDeviceNormal = handler;
}
 
void RPortDms::GetsoaDmsDeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->soaDmsDeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::GetsoaDmsDeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiversoaDmsDeviceNormal)
            {
                m_receiversoaDmsDeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortDms::GetsoaDmsDeviceNormal::" << response.Error().Message();
        }
    }
}
 
void RPortDms::SubscribesoaDmsDriverStatus()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaDmsDriverStatus();
        
        // request subscribe
        auto subscribe = m_interface->soaDmsDriverStatus.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::SubscribesoaDmsDriverStatus::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortDms::SubscribesoaDmsDriverStatus::" << subscribe.Error().Message();
        }
    }
}
 
void RPortDms::StopSubscribesoaDmsDriverStatus()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaDmsDriverStatus.Unsubscribe();
        m_logger.LogVerbose() << "RPortDms::StopSubscribesoaDmsDriverStatus::Unsubscribed";
    }
}
 
void RPortDms::RegistReceiversoaDmsDriverStatus()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaDmsDriverStatusTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaDmsDriverStatus.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::RegistReceiversoaDmsDriverStatus::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortDms::RegistReceiversoaDmsDriverStatus::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortDms::ReceiveFieldsoaDmsDriverStatusTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaDmsDriverStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDmsDriverStatus.GetNewSamples([&](auto samplePtr) {
                RPortDms::ReadValuesoaDmsDriverStatus(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortDms::ReceiveFieldsoaDmsDriverStatus::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortDms::ReceiveFieldsoaDmsDriverStatus::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortDms::ReceiveFieldsoaDmsDriverStatusCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaDmsDriverStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDmsDriverStatus.GetNewSamples([&](auto samplePtr) {
                    RPortDms::ReadValuesoaDmsDriverStatus(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortDms::ReceiveFieldsoaDmsDriverStatus::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortDms::ReceiveFieldsoaDmsDriverStatus::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortDms::ReadValuesoaDmsDriverStatus(ara::com::SamplePtr<eevp::control::proxy::fields::soaDmsDriverStatus::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaDmsDriverStatus)
    {
        m_receiversoaDmsDriverStatus(value);
    }
}
 
void RPortDms::RegistFieldHandlersoaDmsDriverStatus(std::function<void(const eevp::control::proxy::fields::soaDmsDriverStatus::FieldType&)> handler)
{
    m_receiversoaDmsDriverStatus = handler;
}
 
void RPortDms::GetsoaDmsDriverStatus()
{
    if (m_found)
    {
        auto request = m_interface->soaDmsDriverStatus.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::GetsoaDmsDriverStatus::Responded";
            
            auto result = response.Value();
            if (m_receiversoaDmsDriverStatus)
            {
                m_receiversoaDmsDriverStatus(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortDms::GetsoaDmsDriverStatus::" << response.Error().Message();
        }
    }
}
 
void RPortDms::SubscribesoaDmsStatus()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaDmsStatus();
        
        // request subscribe
        auto subscribe = m_interface->soaDmsStatus.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::SubscribesoaDmsStatus::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortDms::SubscribesoaDmsStatus::" << subscribe.Error().Message();
        }
    }
}
 
void RPortDms::StopSubscribesoaDmsStatus()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaDmsStatus.Unsubscribe();
        m_logger.LogVerbose() << "RPortDms::StopSubscribesoaDmsStatus::Unsubscribed";
    }
}
 
void RPortDms::RegistReceiversoaDmsStatus()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaDmsStatusTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaDmsStatus.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::RegistReceiversoaDmsStatus::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortDms::RegistReceiversoaDmsStatus::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortDms::ReceiveFieldsoaDmsStatusTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaDmsStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDmsStatus.GetNewSamples([&](auto samplePtr) {
                RPortDms::ReadValuesoaDmsStatus(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortDms::ReceiveFieldsoaDmsStatus::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortDms::ReceiveFieldsoaDmsStatus::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortDms::ReceiveFieldsoaDmsStatusCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaDmsStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDmsStatus.GetNewSamples([&](auto samplePtr) {
                    RPortDms::ReadValuesoaDmsStatus(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortDms::ReceiveFieldsoaDmsStatus::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortDms::ReceiveFieldsoaDmsStatus::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortDms::ReadValuesoaDmsStatus(ara::com::SamplePtr<eevp::control::proxy::fields::soaDmsStatus::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaDmsStatus)
    {
        m_receiversoaDmsStatus(value);
    }
}
 
void RPortDms::RegistFieldHandlersoaDmsStatus(std::function<void(const eevp::control::proxy::fields::soaDmsStatus::FieldType&)> handler)
{
    m_receiversoaDmsStatus = handler;
}
 
void RPortDms::GetsoaDmsStatus()
{
    if (m_found)
    {
        auto request = m_interface->soaDmsStatus.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::GetsoaDmsStatus::Responded";
            
            auto result = response.Value();
            if (m_receiversoaDmsStatus)
            {
                m_receiversoaDmsStatus(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortDms::GetsoaDmsStatus::" << response.Error().Message();
        }
    }
}
 
void RPortDms::SubscribesoaDmsSwVersion()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaDmsSwVersion();
        
        // request subscribe
        auto subscribe = m_interface->soaDmsSwVersion.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::SubscribesoaDmsSwVersion::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortDms::SubscribesoaDmsSwVersion::" << subscribe.Error().Message();
        }
    }
}
 
void RPortDms::StopSubscribesoaDmsSwVersion()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaDmsSwVersion.Unsubscribe();
        m_logger.LogVerbose() << "RPortDms::StopSubscribesoaDmsSwVersion::Unsubscribed";
    }
}
 
void RPortDms::RegistReceiversoaDmsSwVersion()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaDmsSwVersionTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaDmsSwVersion.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::RegistReceiversoaDmsSwVersion::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortDms::RegistReceiversoaDmsSwVersion::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortDms::ReceiveFieldsoaDmsSwVersionTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaDmsSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDmsSwVersion.GetNewSamples([&](auto samplePtr) {
                RPortDms::ReadValuesoaDmsSwVersion(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortDms::ReceiveFieldsoaDmsSwVersion::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortDms::ReceiveFieldsoaDmsSwVersion::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortDms::ReceiveFieldsoaDmsSwVersionCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaDmsSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaDmsSwVersion.GetNewSamples([&](auto samplePtr) {
                    RPortDms::ReadValuesoaDmsSwVersion(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortDms::ReceiveFieldsoaDmsSwVersion::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortDms::ReceiveFieldsoaDmsSwVersion::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortDms::ReadValuesoaDmsSwVersion(ara::com::SamplePtr<eevp::control::proxy::fields::soaDmsSwVersion::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaDmsSwVersion)
    {
        m_receiversoaDmsSwVersion(value);
    }
}
 
void RPortDms::RegistFieldHandlersoaDmsSwVersion(std::function<void(const eevp::control::proxy::fields::soaDmsSwVersion::FieldType&)> handler)
{
    m_receiversoaDmsSwVersion = handler;
}
 
void RPortDms::GetsoaDmsSwVersion()
{
    if (m_found)
    {
        auto request = m_interface->soaDmsSwVersion.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortDms::GetsoaDmsSwVersion::Responded";
            
            auto result = response.Value();
            if (m_receiversoaDmsSwVersion)
            {
                m_receiversoaDmsSwVersion(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortDms::GetsoaDmsSwVersion::" << response.Error().Message();
        }
    }
}
 
void RPortDms::RequestSetDmsPower(const bool& on)
{
    if (m_found)
    {
        m_interface->SetDmsPower(on);
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace monitoringmanager
 
/// EOF