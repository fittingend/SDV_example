///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rportbcm_exteriorsignal.cpp
/// SOFTWARE COMPONENT NAME           : RPortBCM_ExteriorSignal
/// GENERATED DATE                    : 2025-08-30 10:53:29
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "preventrollback/aa/port/rportbcm_exteriorsignal.h"
 
namespace preventrollback
{
namespace aa
{
namespace port
{
 
RPortBCM_ExteriorSignal::RPortBCM_ExteriorSignal()
    : m_logger(ara::log::CreateLogger("PRRB", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverhornState{nullptr}
    , m_receiverlightState{nullptr}
{
}
 
RPortBCM_ExteriorSignal::~RPortBCM_ExteriorSignal()
{
}
 
void RPortBCM_ExteriorSignal::Start()
{
    m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"PreventRollback/AA/RPortBCM_ExteriorSignal"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::simulation::proxy::BCM_ExteriorSignalProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::simulation::proxy::BCM_ExteriorSignalProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPortBCM_ExteriorSignal::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPortBCM_ExteriorSignal::Terminate()
{
    m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribehornState();
        StopSubscribelightState();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::Terminate::StopFindService";
    }
}
 
void RPortBCM_ExteriorSignal::Find(ara::com::ServiceHandleContainer<eevp::simulation::proxy::BCM_ExteriorSignalProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<eevp::simulation::proxy::BCM_ExteriorSignalProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        SubscribehornState();
        SubscribelightState();
    }
}
 
void RPortBCM_ExteriorSignal::SubscribehornState()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverhornState();
        
        // request subscribe
        auto subscribe = m_interface->hornState.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::SubscribehornState::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortBCM_ExteriorSignal::SubscribehornState::" << subscribe.Error().Message();
        }
    }
}
 
void RPortBCM_ExteriorSignal::StopSubscribehornState()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->hornState.Unsubscribe();
        m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::StopSubscribehornState::Unsubscribed";
    }
}
 
void RPortBCM_ExteriorSignal::RegistReceiverhornState()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldhornStateTriggered();
        };
        
        // regist callback
        auto callback = m_interface->hornState.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::RegistReceiverhornState::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortBCM_ExteriorSignal::RegistReceiverhornState::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortBCM_ExteriorSignal::ReceiveFieldhornStateTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->hornState.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->hornState.GetNewSamples([&](auto samplePtr) {
                RPortBCM_ExteriorSignal::ReadValuehornState(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::ReceiveFieldhornState::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortBCM_ExteriorSignal::ReceiveFieldhornState::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortBCM_ExteriorSignal::ReceiveFieldhornStateCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->hornState.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->hornState.GetNewSamples([&](auto samplePtr) {
                    RPortBCM_ExteriorSignal::ReadValuehornState(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::ReceiveFieldhornState::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortBCM_ExteriorSignal::ReceiveFieldhornState::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortBCM_ExteriorSignal::ReadValuehornState(ara::com::SamplePtr<eevp::simulation::proxy::fields::hornState::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiverhornState)
    {
        m_receiverhornState(value);
    }
}
 
void RPortBCM_ExteriorSignal::RegistFieldHandlerhornState(std::function<void(const eevp::simulation::proxy::fields::hornState::FieldType&)> handler)
{
    m_receiverhornState = handler;
}
 
void RPortBCM_ExteriorSignal::GethornState()
{
    if (m_found)
    {
        auto request = m_interface->hornState.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::GethornState::Responded";
            
            auto result = response.Value();
            if (m_receiverhornState)
            {
                m_receiverhornState(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortBCM_ExteriorSignal::GethornState::" << response.Error().Message();
        }
    }
}
 
void RPortBCM_ExteriorSignal::SubscribelightState()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverlightState();
        
        // request subscribe
        auto subscribe = m_interface->lightState.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::SubscribelightState::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPortBCM_ExteriorSignal::SubscribelightState::" << subscribe.Error().Message();
        }
    }
}
 
void RPortBCM_ExteriorSignal::StopSubscribelightState()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->lightState.Unsubscribe();
        m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::StopSubscribelightState::Unsubscribed";
    }
}
 
void RPortBCM_ExteriorSignal::RegistReceiverlightState()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldlightStateTriggered();
        };
        
        // regist callback
        auto callback = m_interface->lightState.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::RegistReceiverlightState::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPortBCM_ExteriorSignal::RegistReceiverlightState::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPortBCM_ExteriorSignal::ReceiveFieldlightStateTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->lightState.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->lightState.GetNewSamples([&](auto samplePtr) {
                RPortBCM_ExteriorSignal::ReadValuelightState(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::ReceiveFieldlightState::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPortBCM_ExteriorSignal::ReceiveFieldlightState::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortBCM_ExteriorSignal::ReceiveFieldlightStateCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->lightState.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->lightState.GetNewSamples([&](auto samplePtr) {
                    RPortBCM_ExteriorSignal::ReadValuelightState(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::ReceiveFieldlightState::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPortBCM_ExteriorSignal::ReceiveFieldlightState::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortBCM_ExteriorSignal::ReadValuelightState(ara::com::SamplePtr<eevp::simulation::proxy::fields::lightState::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiverlightState)
    {
        m_receiverlightState(value);
    }
}
 
void RPortBCM_ExteriorSignal::RegistFieldHandlerlightState(std::function<void(const eevp::simulation::proxy::fields::lightState::FieldType&)> handler)
{
    m_receiverlightState = handler;
}
 
void RPortBCM_ExteriorSignal::GetlightState()
{
    if (m_found)
    {
        auto request = m_interface->lightState.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortBCM_ExteriorSignal::GetlightState::Responded";
            
            auto result = response.Value();
            if (m_receiverlightState)
            {
                m_receiverlightState(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortBCM_ExteriorSignal::GetlightState::" << response.Error().Message();
        }
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace preventrollback
 
/// EOF