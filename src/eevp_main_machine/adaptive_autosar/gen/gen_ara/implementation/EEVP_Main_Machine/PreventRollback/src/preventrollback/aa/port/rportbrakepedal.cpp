///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rportbrakepedal.cpp
/// SOFTWARE COMPONENT NAME           : RPortBrakePedal
/// GENERATED DATE                    : 2025-09-09 12:00:30
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "preventrollback/aa/port/rportbrakepedal.h"
 
namespace preventrollback
{
namespace aa
{
namespace port
{
 
RPortBrakePedal::RPortBrakePedal()
    : m_logger(ara::log::CreateLogger("PRRB", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_respondernotifyBrakeStatus{nullptr}
    , m_respondernotifyBrakeSwitch{nullptr}
{
}
 
RPortBrakePedal::~RPortBrakePedal()
{
}
 
void RPortBrakePedal::Start()
{
    m_logger.LogVerbose() << "RPortBrakePedal::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"PreventRollback/AA/RPortBrakePedal"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::simulation::proxy::VCS_BrakePedalProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::simulation::proxy::VCS_BrakePedalProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPortBrakePedal::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPortBrakePedal::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPortBrakePedal::Terminate()
{
    m_logger.LogVerbose() << "RPortBrakePedal::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPortBrakePedal::Terminate::StopFindService";
    }
}
 
void RPortBrakePedal::Find(ara::com::ServiceHandleContainer<eevp::simulation::proxy::VCS_BrakePedalProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPortBrakePedal::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPortBrakePedal::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPortBrakePedal::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPortBrakePedal::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<eevp::simulation::proxy::VCS_BrakePedalProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
    }
}
 
void RPortBrakePedal::RequestnotifyBrakeStatus()
{
    if (m_found)
    {
        auto request = m_interface->notifyBrakeStatus();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortBrakePedal::RequestnotifyBrakeStatus::Responded";
            
            auto result = response.Value();
            if (m_respondernotifyBrakeStatus)
            {
                m_respondernotifyBrakeStatus(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortBrakePedal::RequestnotifyBrakeStatus::" << response.Error().Message();
        }
    }
}
 
void RPortBrakePedal::RegistResponseHandlernotifyBrakeStatus(std::function<void(const eevp::simulation::proxy::methods::notifyBrakeStatus::Output&)> handler)
{
    m_respondernotifyBrakeStatus = handler;
}
 
void RPortBrakePedal::RequestnotifyBrakeSwitch()
{
    if (m_found)
    {
        auto request = m_interface->notifyBrakeSwitch();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortBrakePedal::RequestnotifyBrakeSwitch::Responded";
            
            auto result = response.Value();
            if (m_respondernotifyBrakeSwitch)
            {
                m_respondernotifyBrakeSwitch(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortBrakePedal::RequestnotifyBrakeSwitch::" << response.Error().Message();
        }
    }
}
 
void RPortBrakePedal::RegistResponseHandlernotifyBrakeSwitch(std::function<void(const eevp::simulation::proxy::methods::notifyBrakeSwitch::Output&)> handler)
{
    m_respondernotifyBrakeSwitch = handler;
}
 
} /// namespace port
} /// namespace aa
} /// namespace preventrollback
 
/// EOF