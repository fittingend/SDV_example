///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rport_soadbg_wiper.cpp
/// SOFTWARE COMPONENT NAME           : RPort_SOADBG_Wiper
/// GENERATED DATE                    : 2025-08-30 10:53:35
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "soa_debugger/aa/port/rport_soadbg_wiper.h"
 
namespace soa_debugger
{
namespace aa
{
namespace port
{
 
RPort_SOADBG_Wiper::RPort_SOADBG_Wiper()
    : m_logger(ara::log::CreateLogger("SDBG", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiversoaWiperDeviceNormal{nullptr}
    , m_receiversoaWiperStatus{nullptr}
    , m_receiversoaWiperSwVersion{nullptr}
{
}
 
RPort_SOADBG_Wiper::~RPort_SOADBG_Wiper()
{
}
 
void RPort_SOADBG_Wiper::Start()
{
    m_logger.LogVerbose() << "RPort_SOADBG_Wiper::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"SOA_Debugger/AA/RPort_SOADBG_Wiper"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::control::proxy::SoaWiperProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::control::proxy::SoaWiperProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Wiper::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPort_SOADBG_Wiper::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPort_SOADBG_Wiper::Terminate()
{
    m_logger.LogVerbose() << "RPort_SOADBG_Wiper::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribesoaWiperDeviceNormal();
        StopSubscribesoaWiperStatus();
        StopSubscribesoaWiperSwVersion();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPort_SOADBG_Wiper::Terminate::StopFindService";
    }
}
 
void RPort_SOADBG_Wiper::Find(ara::com::ServiceHandleContainer<eevp::control::proxy::SoaWiperProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Wiper::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Wiper::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Wiper::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Wiper::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<eevp::control::proxy::SoaWiperProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        SubscribesoaWiperDeviceNormal();
        SubscribesoaWiperStatus();
        SubscribesoaWiperSwVersion();
    }
}
 
void RPort_SOADBG_Wiper::SubscribesoaWiperDeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaWiperDeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->soaWiperDeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Wiper::SubscribesoaWiperDeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Wiper::SubscribesoaWiperDeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Wiper::StopSubscribesoaWiperDeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaWiperDeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPort_SOADBG_Wiper::StopSubscribesoaWiperDeviceNormal::Unsubscribed";
    }
}
 
void RPort_SOADBG_Wiper::RegistReceiversoaWiperDeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaWiperDeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaWiperDeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Wiper::RegistReceiversoaWiperDeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Wiper::RegistReceiversoaWiperDeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Wiper::ReceiveFieldsoaWiperDeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaWiperDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaWiperDeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPort_SOADBG_Wiper::ReadValuesoaWiperDeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperDeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperDeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_SOADBG_Wiper::ReceiveFieldsoaWiperDeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaWiperDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaWiperDeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPort_SOADBG_Wiper::ReadValuesoaWiperDeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperDeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperDeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_SOADBG_Wiper::ReadValuesoaWiperDeviceNormal(ara::com::SamplePtr<eevp::control::proxy::fields::soaWiperDeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaWiperDeviceNormal)
    {
        m_receiversoaWiperDeviceNormal(value);
    }
}
 
void RPort_SOADBG_Wiper::RegistFieldHandlersoaWiperDeviceNormal(std::function<void(const eevp::control::proxy::fields::soaWiperDeviceNormal::FieldType&)> handler)
{
    m_receiversoaWiperDeviceNormal = handler;
}
 
void RPort_SOADBG_Wiper::GetsoaWiperDeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->soaWiperDeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Wiper::GetsoaWiperDeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiversoaWiperDeviceNormal)
            {
                m_receiversoaWiperDeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Wiper::GetsoaWiperDeviceNormal::" << response.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Wiper::SubscribesoaWiperStatus()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaWiperStatus();
        
        // request subscribe
        auto subscribe = m_interface->soaWiperStatus.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Wiper::SubscribesoaWiperStatus::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Wiper::SubscribesoaWiperStatus::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Wiper::StopSubscribesoaWiperStatus()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaWiperStatus.Unsubscribe();
        m_logger.LogVerbose() << "RPort_SOADBG_Wiper::StopSubscribesoaWiperStatus::Unsubscribed";
    }
}
 
void RPort_SOADBG_Wiper::RegistReceiversoaWiperStatus()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaWiperStatusTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaWiperStatus.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Wiper::RegistReceiversoaWiperStatus::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Wiper::RegistReceiversoaWiperStatus::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Wiper::ReceiveFieldsoaWiperStatusTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaWiperStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaWiperStatus.GetNewSamples([&](auto samplePtr) {
                RPort_SOADBG_Wiper::ReadValuesoaWiperStatus(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperStatus::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperStatus::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_SOADBG_Wiper::ReceiveFieldsoaWiperStatusCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaWiperStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaWiperStatus.GetNewSamples([&](auto samplePtr) {
                    RPort_SOADBG_Wiper::ReadValuesoaWiperStatus(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperStatus::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperStatus::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_SOADBG_Wiper::ReadValuesoaWiperStatus(ara::com::SamplePtr<eevp::control::proxy::fields::soaWiperStatus::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaWiperStatus)
    {
        m_receiversoaWiperStatus(value);
    }
}
 
void RPort_SOADBG_Wiper::RegistFieldHandlersoaWiperStatus(std::function<void(const eevp::control::proxy::fields::soaWiperStatus::FieldType&)> handler)
{
    m_receiversoaWiperStatus = handler;
}
 
void RPort_SOADBG_Wiper::GetsoaWiperStatus()
{
    if (m_found)
    {
        auto request = m_interface->soaWiperStatus.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Wiper::GetsoaWiperStatus::Responded";
            
            auto result = response.Value();
            if (m_receiversoaWiperStatus)
            {
                m_receiversoaWiperStatus(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Wiper::GetsoaWiperStatus::" << response.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Wiper::SubscribesoaWiperSwVersion()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaWiperSwVersion();
        
        // request subscribe
        auto subscribe = m_interface->soaWiperSwVersion.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Wiper::SubscribesoaWiperSwVersion::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Wiper::SubscribesoaWiperSwVersion::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Wiper::StopSubscribesoaWiperSwVersion()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaWiperSwVersion.Unsubscribe();
        m_logger.LogVerbose() << "RPort_SOADBG_Wiper::StopSubscribesoaWiperSwVersion::Unsubscribed";
    }
}
 
void RPort_SOADBG_Wiper::RegistReceiversoaWiperSwVersion()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaWiperSwVersionTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaWiperSwVersion.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Wiper::RegistReceiversoaWiperSwVersion::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Wiper::RegistReceiversoaWiperSwVersion::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Wiper::ReceiveFieldsoaWiperSwVersionTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaWiperSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaWiperSwVersion.GetNewSamples([&](auto samplePtr) {
                RPort_SOADBG_Wiper::ReadValuesoaWiperSwVersion(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperSwVersion::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperSwVersion::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_SOADBG_Wiper::ReceiveFieldsoaWiperSwVersionCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaWiperSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaWiperSwVersion.GetNewSamples([&](auto samplePtr) {
                    RPort_SOADBG_Wiper::ReadValuesoaWiperSwVersion(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperSwVersion::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_SOADBG_Wiper::ReceiveFieldsoaWiperSwVersion::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_SOADBG_Wiper::ReadValuesoaWiperSwVersion(ara::com::SamplePtr<eevp::control::proxy::fields::soaWiperSwVersion::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaWiperSwVersion)
    {
        m_receiversoaWiperSwVersion(value);
    }
}
 
void RPort_SOADBG_Wiper::RegistFieldHandlersoaWiperSwVersion(std::function<void(const eevp::control::proxy::fields::soaWiperSwVersion::FieldType&)> handler)
{
    m_receiversoaWiperSwVersion = handler;
}
 
void RPort_SOADBG_Wiper::GetsoaWiperSwVersion()
{
    if (m_found)
    {
        auto request = m_interface->soaWiperSwVersion.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Wiper::GetsoaWiperSwVersion::Responded";
            
            auto result = response.Value();
            if (m_receiversoaWiperSwVersion)
            {
                m_receiversoaWiperSwVersion(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Wiper::GetsoaWiperSwVersion::" << response.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Wiper::RequestRequestWiperOperation(const eevp::control::SoaWiperMode& mode)
{
    if (m_found)
    {
        m_interface->RequestWiperOperation(mode);
    }
}
 
void RPort_SOADBG_Wiper::RequestSetWiperAutoSpeed(const bool& isAutoSpeed)
{
    if (m_found)
    {
        m_interface->SetWiperAutoSpeed(isAutoSpeed);
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace soa_debugger
 
/// EOF