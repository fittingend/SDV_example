///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rport_soadbg_power.cpp
/// SOFTWARE COMPONENT NAME           : RPort_SOADBG_Power
/// GENERATED DATE                    : 2025-09-05 11:31:35
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "soa_debugger/aa/port/rport_soadbg_power.h"
 
namespace soa_debugger
{
namespace aa
{
namespace port
{
 
RPort_SOADBG_Power::RPort_SOADBG_Power()
    : m_logger(ara::log::CreateLogger("SDBG", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiversoaPowerDeviceNormal{nullptr}
    , m_receiversoaPowerSwVersion{nullptr}
{
}
 
RPort_SOADBG_Power::~RPort_SOADBG_Power()
{
}
 
void RPort_SOADBG_Power::Start()
{
    m_logger.LogVerbose() << "RPort_SOADBG_Power::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"SOA_Debugger/AA/RPort_SOADBG_Power"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::control::proxy::SoaPowerProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::control::proxy::SoaPowerProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Power::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPort_SOADBG_Power::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPort_SOADBG_Power::Terminate()
{
    m_logger.LogVerbose() << "RPort_SOADBG_Power::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribesoaPowerDeviceNormal();
        StopSubscribesoaPowerSwVersion();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPort_SOADBG_Power::Terminate::StopFindService";
    }
}
 
void RPort_SOADBG_Power::Find(ara::com::ServiceHandleContainer<eevp::control::proxy::SoaPowerProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Power::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Power::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Power::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Power::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<eevp::control::proxy::SoaPowerProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        SubscribesoaPowerDeviceNormal();
        SubscribesoaPowerSwVersion();
    }
}
 
void RPort_SOADBG_Power::SubscribesoaPowerDeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaPowerDeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->soaPowerDeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Power::SubscribesoaPowerDeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Power::SubscribesoaPowerDeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Power::StopSubscribesoaPowerDeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaPowerDeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPort_SOADBG_Power::StopSubscribesoaPowerDeviceNormal::Unsubscribed";
    }
}
 
void RPort_SOADBG_Power::RegistReceiversoaPowerDeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaPowerDeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaPowerDeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Power::RegistReceiversoaPowerDeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Power::RegistReceiversoaPowerDeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Power::ReceiveFieldsoaPowerDeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaPowerDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaPowerDeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPort_SOADBG_Power::ReadValuesoaPowerDeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_SOADBG_Power::ReceiveFieldsoaPowerDeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_SOADBG_Power::ReceiveFieldsoaPowerDeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_SOADBG_Power::ReceiveFieldsoaPowerDeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaPowerDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaPowerDeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPort_SOADBG_Power::ReadValuesoaPowerDeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_SOADBG_Power::ReceiveFieldsoaPowerDeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_SOADBG_Power::ReceiveFieldsoaPowerDeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_SOADBG_Power::ReadValuesoaPowerDeviceNormal(ara::com::SamplePtr<eevp::control::proxy::fields::soaPowerDeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaPowerDeviceNormal)
    {
        m_receiversoaPowerDeviceNormal(value);
    }
}
 
void RPort_SOADBG_Power::RegistFieldHandlersoaPowerDeviceNormal(std::function<void(const eevp::control::proxy::fields::soaPowerDeviceNormal::FieldType&)> handler)
{
    m_receiversoaPowerDeviceNormal = handler;
}
 
void RPort_SOADBG_Power::GetsoaPowerDeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->soaPowerDeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Power::GetsoaPowerDeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiversoaPowerDeviceNormal)
            {
                m_receiversoaPowerDeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Power::GetsoaPowerDeviceNormal::" << response.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Power::SubscribesoaPowerSwVersion()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaPowerSwVersion();
        
        // request subscribe
        auto subscribe = m_interface->soaPowerSwVersion.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Power::SubscribesoaPowerSwVersion::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Power::SubscribesoaPowerSwVersion::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Power::StopSubscribesoaPowerSwVersion()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaPowerSwVersion.Unsubscribe();
        m_logger.LogVerbose() << "RPort_SOADBG_Power::StopSubscribesoaPowerSwVersion::Unsubscribed";
    }
}
 
void RPort_SOADBG_Power::RegistReceiversoaPowerSwVersion()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaPowerSwVersionTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaPowerSwVersion.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Power::RegistReceiversoaPowerSwVersion::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Power::RegistReceiversoaPowerSwVersion::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Power::ReceiveFieldsoaPowerSwVersionTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaPowerSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaPowerSwVersion.GetNewSamples([&](auto samplePtr) {
                RPort_SOADBG_Power::ReadValuesoaPowerSwVersion(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_SOADBG_Power::ReceiveFieldsoaPowerSwVersion::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_SOADBG_Power::ReceiveFieldsoaPowerSwVersion::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_SOADBG_Power::ReceiveFieldsoaPowerSwVersionCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaPowerSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaPowerSwVersion.GetNewSamples([&](auto samplePtr) {
                    RPort_SOADBG_Power::ReadValuesoaPowerSwVersion(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_SOADBG_Power::ReceiveFieldsoaPowerSwVersion::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_SOADBG_Power::ReceiveFieldsoaPowerSwVersion::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_SOADBG_Power::ReadValuesoaPowerSwVersion(ara::com::SamplePtr<eevp::control::proxy::fields::soaPowerSwVersion::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaPowerSwVersion)
    {
        m_receiversoaPowerSwVersion(value);
    }
}
 
void RPort_SOADBG_Power::RegistFieldHandlersoaPowerSwVersion(std::function<void(const eevp::control::proxy::fields::soaPowerSwVersion::FieldType&)> handler)
{
    m_receiversoaPowerSwVersion = handler;
}
 
void RPort_SOADBG_Power::GetsoaPowerSwVersion()
{
    if (m_found)
    {
        auto request = m_interface->soaPowerSwVersion.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Power::GetsoaPowerSwVersion::Responded";
            
            auto result = response.Value();
            if (m_receiversoaPowerSwVersion)
            {
                m_receiversoaPowerSwVersion(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Power::GetsoaPowerSwVersion::" << response.Error().Message();
        }
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace soa_debugger
 
/// EOF