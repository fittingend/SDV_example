///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rport_soadbg_hvac.cpp
/// SOFTWARE COMPONENT NAME           : RPort_SOADBG_Hvac
/// GENERATED DATE                    : 2025-09-05 11:31:35
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "soa_debugger/aa/port/rport_soadbg_hvac.h"
 
namespace soa_debugger
{
namespace aa
{
namespace port
{
 
RPort_SOADBG_Hvac::RPort_SOADBG_Hvac()
    : m_logger(ara::log::CreateLogger("SDBG", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverSoaHvacSwVersion{nullptr}
    , m_receiversoaHvacDeviceNormal{nullptr}
    , m_receiversoaHvacSetting{nullptr}
    , m_receiversoaHvacStatus{nullptr}
    , m_receiversoaHvacTemps{nullptr}
{
}
 
RPort_SOADBG_Hvac::~RPort_SOADBG_Hvac()
{
}
 
void RPort_SOADBG_Hvac::Start()
{
    m_logger.LogVerbose() << "RPort_SOADBG_Hvac::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"SOA_Debugger/AA/RPort_SOADBG_Hvac"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::control::proxy::SoaHvacProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::control::proxy::SoaHvacProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Hvac::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPort_SOADBG_Hvac::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPort_SOADBG_Hvac::Terminate()
{
    m_logger.LogVerbose() << "RPort_SOADBG_Hvac::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribeSoaHvacSwVersion();
        StopSubscribesoaHvacDeviceNormal();
        StopSubscribesoaHvacSetting();
        StopSubscribesoaHvacStatus();
        StopSubscribesoaHvacTemps();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPort_SOADBG_Hvac::Terminate::StopFindService";
    }
}
 
void RPort_SOADBG_Hvac::Find(ara::com::ServiceHandleContainer<eevp::control::proxy::SoaHvacProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Hvac::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Hvac::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPort_SOADBG_Hvac::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<eevp::control::proxy::SoaHvacProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        SubscribeSoaHvacSwVersion();
        SubscribesoaHvacDeviceNormal();
        SubscribesoaHvacSetting();
        SubscribesoaHvacStatus();
        SubscribesoaHvacTemps();
    }
}
 
void RPort_SOADBG_Hvac::SubscribeSoaHvacSwVersion()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverSoaHvacSwVersion();
        
        // request subscribe
        auto subscribe = m_interface->SoaHvacSwVersion.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::SubscribeSoaHvacSwVersion::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::SubscribeSoaHvacSwVersion::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::StopSubscribeSoaHvacSwVersion()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->SoaHvacSwVersion.Unsubscribe();
        m_logger.LogVerbose() << "RPort_SOADBG_Hvac::StopSubscribeSoaHvacSwVersion::Unsubscribed";
    }
}
 
void RPort_SOADBG_Hvac::RegistReceiverSoaHvacSwVersion()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldSoaHvacSwVersionTriggered();
        };
        
        // regist callback
        auto callback = m_interface->SoaHvacSwVersion.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::RegistReceiverSoaHvacSwVersion::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::RegistReceiverSoaHvacSwVersion::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::ReceiveFieldSoaHvacSwVersionTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->SoaHvacSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->SoaHvacSwVersion.GetNewSamples([&](auto samplePtr) {
                RPort_SOADBG_Hvac::ReadValueSoaHvacSwVersion(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_SOADBG_Hvac::ReceiveFieldSoaHvacSwVersion::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_SOADBG_Hvac::ReceiveFieldSoaHvacSwVersion::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_SOADBG_Hvac::ReceiveFieldSoaHvacSwVersionCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->SoaHvacSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->SoaHvacSwVersion.GetNewSamples([&](auto samplePtr) {
                    RPort_SOADBG_Hvac::ReadValueSoaHvacSwVersion(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_SOADBG_Hvac::ReceiveFieldSoaHvacSwVersion::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_SOADBG_Hvac::ReceiveFieldSoaHvacSwVersion::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_SOADBG_Hvac::ReadValueSoaHvacSwVersion(ara::com::SamplePtr<eevp::control::proxy::fields::SoaHvacSwVersion::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiverSoaHvacSwVersion)
    {
        m_receiverSoaHvacSwVersion(value);
    }
}
 
void RPort_SOADBG_Hvac::RegistFieldHandlerSoaHvacSwVersion(std::function<void(const eevp::control::proxy::fields::SoaHvacSwVersion::FieldType&)> handler)
{
    m_receiverSoaHvacSwVersion = handler;
}
 
void RPort_SOADBG_Hvac::GetSoaHvacSwVersion()
{
    if (m_found)
    {
        auto request = m_interface->SoaHvacSwVersion.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::GetSoaHvacSwVersion::Responded";
            
            auto result = response.Value();
            if (m_receiverSoaHvacSwVersion)
            {
                m_receiverSoaHvacSwVersion(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::GetSoaHvacSwVersion::" << response.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::SubscribesoaHvacDeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaHvacDeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->soaHvacDeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::SubscribesoaHvacDeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::SubscribesoaHvacDeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::StopSubscribesoaHvacDeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaHvacDeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPort_SOADBG_Hvac::StopSubscribesoaHvacDeviceNormal::Unsubscribed";
    }
}
 
void RPort_SOADBG_Hvac::RegistReceiversoaHvacDeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaHvacDeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaHvacDeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::RegistReceiversoaHvacDeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::RegistReceiversoaHvacDeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::ReceiveFieldsoaHvacDeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaHvacDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacDeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPort_SOADBG_Hvac::ReadValuesoaHvacDeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacDeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacDeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_SOADBG_Hvac::ReceiveFieldsoaHvacDeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaHvacDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacDeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPort_SOADBG_Hvac::ReadValuesoaHvacDeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacDeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacDeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_SOADBG_Hvac::ReadValuesoaHvacDeviceNormal(ara::com::SamplePtr<eevp::control::proxy::fields::soaHvacDeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaHvacDeviceNormal)
    {
        m_receiversoaHvacDeviceNormal(value);
    }
}
 
void RPort_SOADBG_Hvac::RegistFieldHandlersoaHvacDeviceNormal(std::function<void(const eevp::control::proxy::fields::soaHvacDeviceNormal::FieldType&)> handler)
{
    m_receiversoaHvacDeviceNormal = handler;
}
 
void RPort_SOADBG_Hvac::GetsoaHvacDeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->soaHvacDeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::GetsoaHvacDeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiversoaHvacDeviceNormal)
            {
                m_receiversoaHvacDeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::GetsoaHvacDeviceNormal::" << response.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::SubscribesoaHvacSetting()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaHvacSetting();
        
        // request subscribe
        auto subscribe = m_interface->soaHvacSetting.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::SubscribesoaHvacSetting::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::SubscribesoaHvacSetting::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::StopSubscribesoaHvacSetting()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaHvacSetting.Unsubscribe();
        m_logger.LogVerbose() << "RPort_SOADBG_Hvac::StopSubscribesoaHvacSetting::Unsubscribed";
    }
}
 
void RPort_SOADBG_Hvac::RegistReceiversoaHvacSetting()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaHvacSettingTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaHvacSetting.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::RegistReceiversoaHvacSetting::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::RegistReceiversoaHvacSetting::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::ReceiveFieldsoaHvacSettingTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaHvacSetting.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacSetting.GetNewSamples([&](auto samplePtr) {
                RPort_SOADBG_Hvac::ReadValuesoaHvacSetting(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacSetting::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacSetting::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_SOADBG_Hvac::ReceiveFieldsoaHvacSettingCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaHvacSetting.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacSetting.GetNewSamples([&](auto samplePtr) {
                    RPort_SOADBG_Hvac::ReadValuesoaHvacSetting(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacSetting::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacSetting::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_SOADBG_Hvac::ReadValuesoaHvacSetting(ara::com::SamplePtr<eevp::control::proxy::fields::soaHvacSetting::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaHvacSetting)
    {
        m_receiversoaHvacSetting(value);
    }
}
 
void RPort_SOADBG_Hvac::RegistFieldHandlersoaHvacSetting(std::function<void(const eevp::control::proxy::fields::soaHvacSetting::FieldType&)> handler)
{
    m_receiversoaHvacSetting = handler;
}
 
void RPort_SOADBG_Hvac::GetsoaHvacSetting()
{
    if (m_found)
    {
        auto request = m_interface->soaHvacSetting.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::GetsoaHvacSetting::Responded";
            
            auto result = response.Value();
            if (m_receiversoaHvacSetting)
            {
                m_receiversoaHvacSetting(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::GetsoaHvacSetting::" << response.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::SubscribesoaHvacStatus()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaHvacStatus();
        
        // request subscribe
        auto subscribe = m_interface->soaHvacStatus.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::SubscribesoaHvacStatus::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::SubscribesoaHvacStatus::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::StopSubscribesoaHvacStatus()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaHvacStatus.Unsubscribe();
        m_logger.LogVerbose() << "RPort_SOADBG_Hvac::StopSubscribesoaHvacStatus::Unsubscribed";
    }
}
 
void RPort_SOADBG_Hvac::RegistReceiversoaHvacStatus()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaHvacStatusTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaHvacStatus.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::RegistReceiversoaHvacStatus::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::RegistReceiversoaHvacStatus::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::ReceiveFieldsoaHvacStatusTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaHvacStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacStatus.GetNewSamples([&](auto samplePtr) {
                RPort_SOADBG_Hvac::ReadValuesoaHvacStatus(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacStatus::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacStatus::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_SOADBG_Hvac::ReceiveFieldsoaHvacStatusCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaHvacStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacStatus.GetNewSamples([&](auto samplePtr) {
                    RPort_SOADBG_Hvac::ReadValuesoaHvacStatus(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacStatus::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacStatus::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_SOADBG_Hvac::ReadValuesoaHvacStatus(ara::com::SamplePtr<eevp::control::proxy::fields::soaHvacStatus::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaHvacStatus)
    {
        m_receiversoaHvacStatus(value);
    }
}
 
void RPort_SOADBG_Hvac::RegistFieldHandlersoaHvacStatus(std::function<void(const eevp::control::proxy::fields::soaHvacStatus::FieldType&)> handler)
{
    m_receiversoaHvacStatus = handler;
}
 
void RPort_SOADBG_Hvac::GetsoaHvacStatus()
{
    if (m_found)
    {
        auto request = m_interface->soaHvacStatus.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::GetsoaHvacStatus::Responded";
            
            auto result = response.Value();
            if (m_receiversoaHvacStatus)
            {
                m_receiversoaHvacStatus(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::GetsoaHvacStatus::" << response.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::SubscribesoaHvacTemps()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiversoaHvacTemps();
        
        // request subscribe
        auto subscribe = m_interface->soaHvacTemps.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::SubscribesoaHvacTemps::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::SubscribesoaHvacTemps::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::StopSubscribesoaHvacTemps()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaHvacTemps.Unsubscribe();
        m_logger.LogVerbose() << "RPort_SOADBG_Hvac::StopSubscribesoaHvacTemps::Unsubscribed";
    }
}
 
void RPort_SOADBG_Hvac::RegistReceiversoaHvacTemps()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaHvacTempsTriggered();
        };
        
        // regist callback
        auto callback = m_interface->soaHvacTemps.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::RegistReceiversoaHvacTemps::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::RegistReceiversoaHvacTemps::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::ReceiveFieldsoaHvacTempsTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->soaHvacTemps.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacTemps.GetNewSamples([&](auto samplePtr) {
                RPort_SOADBG_Hvac::ReadValuesoaHvacTemps(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacTemps::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacTemps::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_SOADBG_Hvac::ReceiveFieldsoaHvacTempsCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->soaHvacTemps.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaHvacTemps.GetNewSamples([&](auto samplePtr) {
                    RPort_SOADBG_Hvac::ReadValuesoaHvacTemps(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacTemps::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_SOADBG_Hvac::ReceiveFieldsoaHvacTemps::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_SOADBG_Hvac::ReadValuesoaHvacTemps(ara::com::SamplePtr<eevp::control::proxy::fields::soaHvacTemps::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiversoaHvacTemps)
    {
        m_receiversoaHvacTemps(value);
    }
}
 
void RPort_SOADBG_Hvac::RegistFieldHandlersoaHvacTemps(std::function<void(const eevp::control::proxy::fields::soaHvacTemps::FieldType&)> handler)
{
    m_receiversoaHvacTemps = handler;
}
 
void RPort_SOADBG_Hvac::GetsoaHvacTemps()
{
    if (m_found)
    {
        auto request = m_interface->soaHvacTemps.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_SOADBG_Hvac::GetsoaHvacTemps::Responded";
            
            auto result = response.Value();
            if (m_receiversoaHvacTemps)
            {
                m_receiversoaHvacTemps(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_SOADBG_Hvac::GetsoaHvacTemps::" << response.Error().Message();
        }
    }
}
 
void RPort_SOADBG_Hvac::RequestSetAcOnOff(const bool& on)
{
    if (m_found)
    {
        m_interface->SetAcOnOff(on);
    }
}
 
void RPort_SOADBG_Hvac::RequestSetAirSource(const eevp::control::SoaHvacAirSource& src)
{
    if (m_found)
    {
        m_interface->SetAirSource(src);
    }
}
 
void RPort_SOADBG_Hvac::RequestSetAutoTemp(const bool& on)
{
    if (m_found)
    {
        m_interface->SetAutoTemp(on);
    }
}
 
void RPort_SOADBG_Hvac::RequestSetBackwardDefrostOn(const bool& on)
{
    if (m_found)
    {
        m_interface->SetBackwardDefrostOn(on);
    }
}
 
void RPort_SOADBG_Hvac::RequestSetBlowingDirection(const eevp::control::SoaHvacBlowDir& dir)
{
    if (m_found)
    {
        m_interface->SetBlowingDirection(dir);
    }
}
 
void RPort_SOADBG_Hvac::RequestSetBlowingForce(const std::uint8_t& force)
{
    if (m_found)
    {
        m_interface->SetBlowingForce(force);
    }
}
 
void RPort_SOADBG_Hvac::RequestSetDriverTemp(const std::int8_t& temperature)
{
    if (m_found)
    {
        m_interface->SetDriverTemp(temperature);
    }
}
 
void RPort_SOADBG_Hvac::RequestSetForwardDefrostOn(const bool& on)
{
    if (m_found)
    {
        m_interface->SetForwardDefrostOn(on);
    }
}
 
void RPort_SOADBG_Hvac::RequestSetHvacPower(const bool& on)
{
    if (m_found)
    {
        m_interface->SetHvacPower(on);
    }
}
 
void RPort_SOADBG_Hvac::RequestSetPassengerTemp(const std::int8_t& temperature)
{
    if (m_found)
    {
        m_interface->SetPassengerTemp(temperature);
    }
}
 
void RPort_SOADBG_Hvac::RequestSetSync(const bool& on)
{
    if (m_found)
    {
        m_interface->SetSync(on);
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace soa_debugger
 
/// EOF