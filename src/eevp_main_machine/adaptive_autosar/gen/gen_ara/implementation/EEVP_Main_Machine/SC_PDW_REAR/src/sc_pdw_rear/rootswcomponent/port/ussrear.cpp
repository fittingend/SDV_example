///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : ussrear.cpp
/// SOFTWARE COMPONENT NAME           : UssRear
/// GENERATED DATE                    : 2025-09-05 11:31:34
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "sc_pdw_rear/rootswcomponent/port/ussrear.h"
 
namespace sc_pdw_rear
{
namespace rootswcomponent
{
namespace port
{
 
UssRear::UssRear()
    : m_logger(ara::log::CreateLogger("PDWR", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverUssRear{nullptr}
{
}
 
UssRear::~UssRear()
{
}
 
void UssRear::Start()
{
    m_logger.LogVerbose() << "UssRear::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"SC_PDW_REAR/RootSwComponent/UssRear"};
    auto handler = [this](ara::com::ServiceHandleContainer<zone::input::service::proxy::Zone_UssRear_InputProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = zone::input::service::proxy::Zone_UssRear_InputProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "UssRear::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "UssRear::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void UssRear::Terminate()
{
    m_logger.LogVerbose() << "UssRear::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        // stop subscribe
        StopSubscribeUssRear();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "UssRear::Terminate::StopFindService";
    }
}
 
void UssRear::Find(ara::com::ServiceHandleContainer<zone::input::service::proxy::Zone_UssRear_InputProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "UssRear::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "UssRear::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "UssRear::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "UssRear::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<zone::input::service::proxy::Zone_UssRear_InputProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe events
        SubscribeUssRear();
    }
}
 
void UssRear::SubscribeUssRear()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverUssRear();
        
        // request subscribe
        auto subscribe = m_interface->UssRear.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "UssRear::SubscribeUssRear::Subscribed";
        }
        else
        {
            m_logger.LogError() << "UssRear::SubscribeUssRear::" << subscribe.Error().Message();
        }
    }
}
 
void UssRear::StopSubscribeUssRear()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->UssRear.Unsubscribe();
        m_logger.LogVerbose() << "UssRear::StopSubscribeUssRear::Unsubscribed";
    }
}
 
void UssRear::RegistReceiverUssRear()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventUssRearTriggered();
        };
        
        // regist callback
        auto callback = m_interface->UssRear.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "UssRear::RegistReceiverUssRear::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "UssRear::RegistReceiverUssRear::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void UssRear::ReceiveEventUssRearTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->UssRear.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->UssRear.GetNewSamples([&](auto samplePtr) {
                UssRear::ReadDataUssRear(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "UssRear::ReceiveEventUssRear::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "UssRear::ReceiveEventUssRear::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void UssRear::ReceiveEventUssRearCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->UssRear.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->UssRear.GetNewSamples([&](auto samplePtr) {
                    UssRear::ReadDataUssRear(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "UssRear::ReceiveEventUssRear::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "UssRear::ReceiveEventUssRear::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void UssRear::ReadDataUssRear(ara::com::SamplePtr<zone::input::service::proxy::events::UssRear::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiverUssRear)
    {
        m_receiverUssRear(data);
    }
}
 
void UssRear::RegistEventHandlerUssRear(std::function<void(const zone::input::service::proxy::events::UssRear::SampleType&)> handler)
{
    m_receiverUssRear = handler;
}
 
} /// namespace port
} /// namespace rootswcomponent
} /// namespace sc_pdw_rear
 
/// EOF