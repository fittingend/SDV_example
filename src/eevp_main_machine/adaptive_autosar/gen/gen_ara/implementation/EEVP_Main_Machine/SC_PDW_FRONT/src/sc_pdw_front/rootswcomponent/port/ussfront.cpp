///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : ussfront.cpp
/// SOFTWARE COMPONENT NAME           : UssFront
/// GENERATED DATE                    : 2025-09-05 11:31:34
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "sc_pdw_front/rootswcomponent/port/ussfront.h"
 
namespace sc_pdw_front
{
namespace rootswcomponent
{
namespace port
{
 
UssFront::UssFront()
    : m_logger(ara::log::CreateLogger("PDWF", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverUssFront{nullptr}
{
}
 
UssFront::~UssFront()
{
}
 
void UssFront::Start()
{
    m_logger.LogVerbose() << "UssFront::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"SC_PDW_FRONT/RootSwComponent/UssFront"};
    auto handler = [this](ara::com::ServiceHandleContainer<zone::input::service::proxy::Zone_UssFront_InputProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = zone::input::service::proxy::Zone_UssFront_InputProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "UssFront::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "UssFront::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void UssFront::Terminate()
{
    m_logger.LogVerbose() << "UssFront::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        // stop subscribe
        StopSubscribeUssFront();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "UssFront::Terminate::StopFindService";
    }
}
 
void UssFront::Find(ara::com::ServiceHandleContainer<zone::input::service::proxy::Zone_UssFront_InputProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "UssFront::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "UssFront::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "UssFront::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "UssFront::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<zone::input::service::proxy::Zone_UssFront_InputProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe events
        SubscribeUssFront();
    }
}
 
void UssFront::SubscribeUssFront()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverUssFront();
        
        // request subscribe
        auto subscribe = m_interface->UssFront.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "UssFront::SubscribeUssFront::Subscribed";
        }
        else
        {
            m_logger.LogError() << "UssFront::SubscribeUssFront::" << subscribe.Error().Message();
        }
    }
}
 
void UssFront::StopSubscribeUssFront()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->UssFront.Unsubscribe();
        m_logger.LogVerbose() << "UssFront::StopSubscribeUssFront::Unsubscribed";
    }
}
 
void UssFront::RegistReceiverUssFront()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventUssFrontTriggered();
        };
        
        // regist callback
        auto callback = m_interface->UssFront.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "UssFront::RegistReceiverUssFront::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "UssFront::RegistReceiverUssFront::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void UssFront::ReceiveEventUssFrontTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->UssFront.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->UssFront.GetNewSamples([&](auto samplePtr) {
                UssFront::ReadDataUssFront(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "UssFront::ReceiveEventUssFront::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "UssFront::ReceiveEventUssFront::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void UssFront::ReceiveEventUssFrontCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->UssFront.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->UssFront.GetNewSamples([&](auto samplePtr) {
                    UssFront::ReadDataUssFront(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "UssFront::ReceiveEventUssFront::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "UssFront::ReceiveEventUssFront::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void UssFront::ReadDataUssFront(ara::com::SamplePtr<zone::input::service::proxy::events::UssFront::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiverUssFront)
    {
        m_receiverUssFront(data);
    }
}
 
void UssFront::RegistEventHandlerUssFront(std::function<void(const zone::input::service::proxy::events::UssFront::SampleType&)> handler)
{
    m_receiverUssFront = handler;
}
 
} /// namespace port
} /// namespace rootswcomponent
} /// namespace sc_pdw_front
 
/// EOF