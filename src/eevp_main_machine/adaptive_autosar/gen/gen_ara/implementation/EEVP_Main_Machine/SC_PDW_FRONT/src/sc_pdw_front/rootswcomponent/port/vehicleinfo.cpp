///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : vehicleinfo.cpp
/// SOFTWARE COMPONENT NAME           : VehicleInfo
/// GENERATED DATE                    : 2025-08-30 10:53:30
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "sc_pdw_front/rootswcomponent/port/vehicleinfo.h"
 
namespace sc_pdw_front
{
namespace rootswcomponent
{
namespace port
{
 
VehicleInfo::VehicleInfo()
    : m_logger(ara::log::CreateLogger("PDWF", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverVehicleInfo{nullptr}
{
}
 
VehicleInfo::~VehicleInfo()
{
}
 
void VehicleInfo::Start()
{
    m_logger.LogVerbose() << "VehicleInfo::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"SC_PDW_FRONT/RootSwComponent/VehicleInfo"};
    auto handler = [this](ara::com::ServiceHandleContainer<zone::input::service::proxy::Zone_VehicleInfo_InputProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = zone::input::service::proxy::Zone_VehicleInfo_InputProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "VehicleInfo::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "VehicleInfo::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void VehicleInfo::Terminate()
{
    m_logger.LogVerbose() << "VehicleInfo::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        // stop subscribe
        StopSubscribeVehicleInfo();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "VehicleInfo::Terminate::StopFindService";
    }
}
 
void VehicleInfo::Find(ara::com::ServiceHandleContainer<zone::input::service::proxy::Zone_VehicleInfo_InputProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "VehicleInfo::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "VehicleInfo::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "VehicleInfo::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "VehicleInfo::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<zone::input::service::proxy::Zone_VehicleInfo_InputProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe events
        SubscribeVehicleInfo();
    }
}
 
void VehicleInfo::SubscribeVehicleInfo()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverVehicleInfo();
        
        // request subscribe
        auto subscribe = m_interface->VehicleInfo.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "VehicleInfo::SubscribeVehicleInfo::Subscribed";
        }
        else
        {
            m_logger.LogError() << "VehicleInfo::SubscribeVehicleInfo::" << subscribe.Error().Message();
        }
    }
}
 
void VehicleInfo::StopSubscribeVehicleInfo()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->VehicleInfo.Unsubscribe();
        m_logger.LogVerbose() << "VehicleInfo::StopSubscribeVehicleInfo::Unsubscribed";
    }
}
 
void VehicleInfo::RegistReceiverVehicleInfo()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventVehicleInfoTriggered();
        };
        
        // regist callback
        auto callback = m_interface->VehicleInfo.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "VehicleInfo::RegistReceiverVehicleInfo::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "VehicleInfo::RegistReceiverVehicleInfo::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void VehicleInfo::ReceiveEventVehicleInfoTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->VehicleInfo.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->VehicleInfo.GetNewSamples([&](auto samplePtr) {
                VehicleInfo::ReadDataVehicleInfo(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "VehicleInfo::ReceiveEventVehicleInfo::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "VehicleInfo::ReceiveEventVehicleInfo::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void VehicleInfo::ReceiveEventVehicleInfoCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->VehicleInfo.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->VehicleInfo.GetNewSamples([&](auto samplePtr) {
                    VehicleInfo::ReadDataVehicleInfo(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "VehicleInfo::ReceiveEventVehicleInfo::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "VehicleInfo::ReceiveEventVehicleInfo::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void VehicleInfo::ReadDataVehicleInfo(ara::com::SamplePtr<zone::input::service::proxy::events::VehicleInfo::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiverVehicleInfo)
    {
        m_receiverVehicleInfo(data);
    }
}
 
void VehicleInfo::RegistEventHandlerVehicleInfo(std::function<void(const zone::input::service::proxy::events::VehicleInfo::SampleType&)> handler)
{
    m_receiverVehicleInfo = handler;
}
 
} /// namespace port
} /// namespace rootswcomponent
} /// namespace sc_pdw_front
 
/// EOF