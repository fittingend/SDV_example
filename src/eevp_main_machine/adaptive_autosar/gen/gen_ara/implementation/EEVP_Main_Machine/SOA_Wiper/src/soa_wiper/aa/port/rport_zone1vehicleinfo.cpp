///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rport_zone1vehicleinfo.cpp
/// SOFTWARE COMPONENT NAME           : RPort_Zone1VehicleInfo
/// GENERATED DATE                    : 2025-08-30 10:53:44
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "soa_wiper/aa/port/rport_zone1vehicleinfo.h"
 
namespace soa_wiper
{
namespace aa
{
namespace port
{
 
RPort_Zone1VehicleInfo::RPort_Zone1VehicleInfo()
    : m_logger(ara::log::CreateLogger("SWIP", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverVehicleInfo{nullptr}
{
}
 
RPort_Zone1VehicleInfo::~RPort_Zone1VehicleInfo()
{
}
 
void RPort_Zone1VehicleInfo::Start()
{
    m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"SOA_Wiper/AA/RPort_Zone1VehicleInfo"};
    auto handler = [this](ara::com::ServiceHandleContainer<zone::input::service::proxy::Zone_VehicleInfo_InputProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = zone::input::service::proxy::Zone_VehicleInfo_InputProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPort_Zone1VehicleInfo::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPort_Zone1VehicleInfo::Terminate()
{
    m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        // stop subscribe
        StopSubscribeVehicleInfo();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::Terminate::StopFindService";
    }
}
 
void RPort_Zone1VehicleInfo::Find(ara::com::ServiceHandleContainer<zone::input::service::proxy::Zone_VehicleInfo_InputProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<zone::input::service::proxy::Zone_VehicleInfo_InputProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe events
        SubscribeVehicleInfo();
    }
}
 
void RPort_Zone1VehicleInfo::SubscribeVehicleInfo()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverVehicleInfo();
        
        // request subscribe
        auto subscribe = m_interface->VehicleInfo.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::SubscribeVehicleInfo::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone1VehicleInfo::SubscribeVehicleInfo::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_Zone1VehicleInfo::StopSubscribeVehicleInfo()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->VehicleInfo.Unsubscribe();
        m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::StopSubscribeVehicleInfo::Unsubscribed";
    }
}
 
void RPort_Zone1VehicleInfo::RegistReceiverVehicleInfo()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveEventVehicleInfoTriggered();
        };
        
        // regist callback
        auto callback = m_interface->VehicleInfo.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::RegistReceiverVehicleInfo::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone1VehicleInfo::RegistReceiverVehicleInfo::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_Zone1VehicleInfo::ReceiveEventVehicleInfoTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->VehicleInfo.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->VehicleInfo.GetNewSamples([&](auto samplePtr) {
                RPort_Zone1VehicleInfo::ReadDataVehicleInfo(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::ReceiveEventVehicleInfo::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_Zone1VehicleInfo::ReceiveEventVehicleInfo::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_Zone1VehicleInfo::ReceiveEventVehicleInfoCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->VehicleInfo.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->VehicleInfo.GetNewSamples([&](auto samplePtr) {
                    RPort_Zone1VehicleInfo::ReadDataVehicleInfo(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_Zone1VehicleInfo::ReceiveEventVehicleInfo::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_Zone1VehicleInfo::ReceiveEventVehicleInfo::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_Zone1VehicleInfo::ReadDataVehicleInfo(ara::com::SamplePtr<zone::input::service::proxy::events::VehicleInfo::SampleType const> samplePtr)
{
    auto data = *samplePtr.Get();
    if (m_receiverVehicleInfo)
    {
        m_receiverVehicleInfo(data);
    }
}
 
void RPort_Zone1VehicleInfo::RegistEventHandlerVehicleInfo(std::function<void(const zone::input::service::proxy::events::VehicleInfo::SampleType&)> handler)
{
    m_receiverVehicleInfo = handler;
}
 
} /// namespace port
} /// namespace aa
} /// namespace soa_wiper
 
/// EOF