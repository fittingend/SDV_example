///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rport_zone3rsva.cpp
/// SOFTWARE COMPONENT NAME           : RPort_Zone3RsvA
/// GENERATED DATE                    : 2025-08-30 10:53:40
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "soa_power/aa/port/rport_zone3rsva.h"
 
namespace soa_power
{
namespace aa
{
namespace port
{
 
RPort_Zone3RsvA::RPort_Zone3RsvA()
    : m_logger(ara::log::CreateLogger("SPWR", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverzone3DeviceNormal{nullptr}
{
}
 
RPort_Zone3RsvA::~RPort_Zone3RsvA()
{
}
 
void RPort_Zone3RsvA::Start()
{
    m_logger.LogVerbose() << "RPort_Zone3RsvA::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"SOA_Power/AA/RPort_Zone3RsvA"};
    auto handler = [this](ara::com::ServiceHandleContainer<zone3::reserved::a::proxy::Zone3RsvAProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = zone3::reserved::a::proxy::Zone3RsvAProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPort_Zone3RsvA::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPort_Zone3RsvA::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPort_Zone3RsvA::Terminate()
{
    m_logger.LogVerbose() << "RPort_Zone3RsvA::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribezone3DeviceNormal();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPort_Zone3RsvA::Terminate::StopFindService";
    }
}
 
void RPort_Zone3RsvA::Find(ara::com::ServiceHandleContainer<zone3::reserved::a::proxy::Zone3RsvAProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPort_Zone3RsvA::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPort_Zone3RsvA::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPort_Zone3RsvA::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPort_Zone3RsvA::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<zone3::reserved::a::proxy::Zone3RsvAProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        Subscribezone3DeviceNormal();
    }
}
 
void RPort_Zone3RsvA::Subscribezone3DeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverzone3DeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->zone3DeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone3RsvA::Subscribezone3DeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone3RsvA::Subscribezone3DeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_Zone3RsvA::StopSubscribezone3DeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->zone3DeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPort_Zone3RsvA::StopSubscribezone3DeviceNormal::Unsubscribed";
    }
}
 
void RPort_Zone3RsvA::RegistReceiverzone3DeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldzone3DeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->zone3DeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone3RsvA::RegistReceiverzone3DeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone3RsvA::RegistReceiverzone3DeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_Zone3RsvA::ReceiveFieldzone3DeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->zone3DeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone3DeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPort_Zone3RsvA::ReadValuezone3DeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_Zone3RsvA::ReceiveFieldzone3DeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_Zone3RsvA::ReceiveFieldzone3DeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_Zone3RsvA::ReceiveFieldzone3DeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->zone3DeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone3DeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPort_Zone3RsvA::ReadValuezone3DeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_Zone3RsvA::ReceiveFieldzone3DeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_Zone3RsvA::ReceiveFieldzone3DeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_Zone3RsvA::ReadValuezone3DeviceNormal(ara::com::SamplePtr<zone3::reserved::a::proxy::fields::zone3DeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiverzone3DeviceNormal)
    {
        m_receiverzone3DeviceNormal(value);
    }
}
 
void RPort_Zone3RsvA::RegistFieldHandlerzone3DeviceNormal(std::function<void(const zone3::reserved::a::proxy::fields::zone3DeviceNormal::FieldType&)> handler)
{
    m_receiverzone3DeviceNormal = handler;
}
 
void RPort_Zone3RsvA::Getzone3DeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->zone3DeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone3RsvA::Getzone3DeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiverzone3DeviceNormal)
            {
                m_receiverzone3DeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_Zone3RsvA::Getzone3DeviceNormal::" << response.Error().Message();
        }
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace soa_power
 
/// EOF