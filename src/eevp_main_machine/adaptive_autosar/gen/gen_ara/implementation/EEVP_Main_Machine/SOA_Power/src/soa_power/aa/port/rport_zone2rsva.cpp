///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rport_zone2rsva.cpp
/// SOFTWARE COMPONENT NAME           : RPort_Zone2RsvA
/// GENERATED DATE                    : 2025-09-05 11:31:37
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "soa_power/aa/port/rport_zone2rsva.h"
 
namespace soa_power
{
namespace aa
{
namespace port
{
 
RPort_Zone2RsvA::RPort_Zone2RsvA()
    : m_logger(ara::log::CreateLogger("SPWR", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverzone2DeviceNormal{nullptr}
{
}
 
RPort_Zone2RsvA::~RPort_Zone2RsvA()
{
}
 
void RPort_Zone2RsvA::Start()
{
    m_logger.LogVerbose() << "RPort_Zone2RsvA::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"SOA_Power/AA/RPort_Zone2RsvA"};
    auto handler = [this](ara::com::ServiceHandleContainer<zone2::reserved::a::proxy::Zone2RsvAProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = zone2::reserved::a::proxy::Zone2RsvAProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPort_Zone2RsvA::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPort_Zone2RsvA::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPort_Zone2RsvA::Terminate()
{
    m_logger.LogVerbose() << "RPort_Zone2RsvA::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribezone2DeviceNormal();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPort_Zone2RsvA::Terminate::StopFindService";
    }
}
 
void RPort_Zone2RsvA::Find(ara::com::ServiceHandleContainer<zone2::reserved::a::proxy::Zone2RsvAProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPort_Zone2RsvA::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPort_Zone2RsvA::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPort_Zone2RsvA::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPort_Zone2RsvA::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<zone2::reserved::a::proxy::Zone2RsvAProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        Subscribezone2DeviceNormal();
    }
}
 
void RPort_Zone2RsvA::Subscribezone2DeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverzone2DeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->zone2DeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone2RsvA::Subscribezone2DeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone2RsvA::Subscribezone2DeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_Zone2RsvA::StopSubscribezone2DeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->zone2DeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPort_Zone2RsvA::StopSubscribezone2DeviceNormal::Unsubscribed";
    }
}
 
void RPort_Zone2RsvA::RegistReceiverzone2DeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldzone2DeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->zone2DeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone2RsvA::RegistReceiverzone2DeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone2RsvA::RegistReceiverzone2DeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_Zone2RsvA::ReceiveFieldzone2DeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->zone2DeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone2DeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPort_Zone2RsvA::ReadValuezone2DeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_Zone2RsvA::ReceiveFieldzone2DeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_Zone2RsvA::ReceiveFieldzone2DeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_Zone2RsvA::ReceiveFieldzone2DeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->zone2DeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone2DeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPort_Zone2RsvA::ReadValuezone2DeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_Zone2RsvA::ReceiveFieldzone2DeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_Zone2RsvA::ReceiveFieldzone2DeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_Zone2RsvA::ReadValuezone2DeviceNormal(ara::com::SamplePtr<zone2::reserved::a::proxy::fields::zone2DeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiverzone2DeviceNormal)
    {
        m_receiverzone2DeviceNormal(value);
    }
}
 
void RPort_Zone2RsvA::RegistFieldHandlerzone2DeviceNormal(std::function<void(const zone2::reserved::a::proxy::fields::zone2DeviceNormal::FieldType&)> handler)
{
    m_receiverzone2DeviceNormal = handler;
}
 
void RPort_Zone2RsvA::Getzone2DeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->zone2DeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone2RsvA::Getzone2DeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiverzone2DeviceNormal)
            {
                m_receiverzone2DeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_Zone2RsvA::Getzone2DeviceNormal::" << response.Error().Message();
        }
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace soa_power
 
/// EOF