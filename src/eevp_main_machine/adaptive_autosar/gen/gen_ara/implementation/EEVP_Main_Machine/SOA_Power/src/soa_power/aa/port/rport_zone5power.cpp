///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rport_zone5power.cpp
/// SOFTWARE COMPONENT NAME           : RPort_Zone5Power
/// GENERATED DATE                    : 2025-08-30 10:53:40
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "soa_power/aa/port/rport_zone5power.h"
 
namespace soa_power
{
namespace aa
{
namespace port
{
 
RPort_Zone5Power::RPort_Zone5Power()
    : m_logger(ara::log::CreateLogger("SPWR", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_receiverzone5PowerDeviceNormal{nullptr}
{
}
 
RPort_Zone5Power::~RPort_Zone5Power()
{
}
 
void RPort_Zone5Power::Start()
{
    m_logger.LogVerbose() << "RPort_Zone5Power::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"SOA_Power/AA/RPort_Zone5Power"};
    auto handler = [this](ara::com::ServiceHandleContainer<zone5::power::control::proxy::Zone5PowerProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = zone5::power::control::proxy::Zone5PowerProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPort_Zone5Power::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPort_Zone5Power::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPort_Zone5Power::Terminate()
{
    m_logger.LogVerbose() << "RPort_Zone5Power::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        StopSubscribezone5PowerDeviceNormal();
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPort_Zone5Power::Terminate::StopFindService";
    }
}
 
void RPort_Zone5Power::Find(ara::com::ServiceHandleContainer<zone5::power::control::proxy::Zone5PowerProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPort_Zone5Power::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPort_Zone5Power::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPort_Zone5Power::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPort_Zone5Power::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<zone5::power::control::proxy::Zone5PowerProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
        // subscribe field notifications
        Subscribezone5PowerDeviceNormal();
    }
}
 
void RPort_Zone5Power::Subscribezone5PowerDeviceNormal()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // RegistReceiverzone5PowerDeviceNormal();
        
        // request subscribe
        auto subscribe = m_interface->zone5PowerDeviceNormal.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Power::Subscribezone5PowerDeviceNormal::Subscribed";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Power::Subscribezone5PowerDeviceNormal::" << subscribe.Error().Message();
        }
    }
}
 
void RPort_Zone5Power::StopSubscribezone5PowerDeviceNormal()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->zone5PowerDeviceNormal.Unsubscribe();
        m_logger.LogVerbose() << "RPort_Zone5Power::StopSubscribezone5PowerDeviceNormal::Unsubscribed";
    }
}
 
void RPort_Zone5Power::RegistReceiverzone5PowerDeviceNormal()
{
    if (m_found)
    {
        // set callback
        auto receiver = [this]() -> void {
            return ReceiveFieldzone5PowerDeviceNormalTriggered();
        };
        
        // regist callback
        auto callback = m_interface->zone5PowerDeviceNormal.SetReceiveHandler(receiver);
        if (callback.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Power::RegistReceiverzone5PowerDeviceNormal::SetReceiveHandler";
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Power::RegistReceiverzone5PowerDeviceNormal::SetReceiveHandler::" << callback.Error().Message();
        }
    }
}
 
void RPort_Zone5Power::ReceiveFieldzone5PowerDeviceNormalTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_interface->zone5PowerDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone5PowerDeviceNormal.GetNewSamples([&](auto samplePtr) {
                RPort_Zone5Power::ReadValuezone5PowerDeviceNormal(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPort_Zone5Power::ReceiveFieldzone5PowerDeviceNormal::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogError() << "RPort_Zone5Power::ReceiveFieldzone5PowerDeviceNormal::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPort_Zone5Power::ReceiveFieldzone5PowerDeviceNormalCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        
        if (m_found)
        {
            if (m_interface->zone5PowerDeviceNormal.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->zone5PowerDeviceNormal.GetNewSamples([&](auto samplePtr) {
                    RPort_Zone5Power::ReadValuezone5PowerDeviceNormal(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPort_Zone5Power::ReceiveFieldzone5PowerDeviceNormal::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogError() << "RPort_Zone5Power::ReceiveFieldzone5PowerDeviceNormal::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPort_Zone5Power::ReadValuezone5PowerDeviceNormal(ara::com::SamplePtr<zone5::power::control::proxy::fields::zone5PowerDeviceNormal::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    if (m_receiverzone5PowerDeviceNormal)
    {
        m_receiverzone5PowerDeviceNormal(value);
    }
}
 
void RPort_Zone5Power::RegistFieldHandlerzone5PowerDeviceNormal(std::function<void(const zone5::power::control::proxy::fields::zone5PowerDeviceNormal::FieldType&)> handler)
{
    m_receiverzone5PowerDeviceNormal = handler;
}
 
void RPort_Zone5Power::Getzone5PowerDeviceNormal()
{
    if (m_found)
    {
        auto request = m_interface->zone5PowerDeviceNormal.Get();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPort_Zone5Power::Getzone5PowerDeviceNormal::Responded";
            
            auto result = response.Value();
            if (m_receiverzone5PowerDeviceNormal)
            {
                m_receiverzone5PowerDeviceNormal(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPort_Zone5Power::Getzone5PowerDeviceNormal::" << response.Error().Message();
        }
    }
}
 
} /// namespace port
} /// namespace aa
} /// namespace soa_power
 
/// EOF