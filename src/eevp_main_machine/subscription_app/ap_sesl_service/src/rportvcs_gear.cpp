///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rportvcs_gear.cpp
/// SOFTWARE COMPONENT NAME           : RPortVCS_Gear
/// GENERATED DATE                    : 2025-08-30 10:53:29
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "preventrollback/aa/port/rportvcs_gear.h"
 
namespace preventrollback
{
namespace aa
{
namespace port
{
 
RPortVCS_Gear::RPortVCS_Gear()
    : m_logger(ara::log::CreateLogger("PRRB", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
    , m_respondernotifyGear{nullptr}
    , m_respondersetTarget{nullptr}
{
}
 
RPortVCS_Gear::~RPortVCS_Gear()
{
}
 
void RPortVCS_Gear::Start()
{
    m_logger.LogVerbose() << "RPortVCS_Gear::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"PreventRollback/AA/RPortVCS_Gear"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::simulation::proxy::VCS_GearProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::simulation::proxy::VCS_GearProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPortVCS_Gear::Start::StartFindService";
    }
    else
    {
        m_logger.LogError() << "RPortVCS_Gear::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPortVCS_Gear::Terminate()
{
    m_logger.LogVerbose() << "RPortVCS_Gear::Terminate";
    
    // stop port
    m_running = false;
    
    // clear service proxy
    if (m_interface)
    {
        
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPortVCS_Gear::Terminate::StopFindService";
    }
}
 
void RPortVCS_Gear::Find(ara::com::ServiceHandleContainer<eevp::simulation::proxy::VCS_GearProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPortVCS_Gear::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPortVCS_Gear::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPortVCS_Gear::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPortVCS_Gear::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_unique<eevp::simulation::proxy::VCS_GearProxy>(handles[0]);
        m_findHandle = std::make_unique<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        
    }
}
 
void RPortVCS_Gear::RequestnotifyGear()
{
    if (m_found)
    {
        auto request = m_interface->notifyGear();
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortVCS_Gear::RequestnotifyGear::Responded";
            
            auto result = response.Value();
            if (m_respondernotifyGear)
            {
                m_respondernotifyGear(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortVCS_Gear::RequestnotifyGear::" << response.Error().Message();
        }
    }
}
 
void RPortVCS_Gear::RegistResponseHandlernotifyGear(std::function<void(const eevp::simulation::proxy::methods::notifyGear::Output&)> handler)
{
    m_respondernotifyGear = handler;
}
 
void RPortVCS_Gear::RequestsetTarget(const eevp::simulation::type::VCS_Gear& targetGear)
{
    if (m_found)
    {
        auto request = m_interface->setTarget(targetGear);
        request.wait();
        auto response = request.GetResult();
        if (response.HasValue())
        {
            m_logger.LogVerbose() << "RPortVCS_Gear::RequestsetTarget::Responded";
            
            auto result = response.Value();
            if (m_respondersetTarget)
            {
                m_respondersetTarget(result);
            }
        }
        else
        {
            m_logger.LogError() << "RPortVCS_Gear::RequestsetTarget::" << response.Error().Message();
        }
    }
}
 
void RPortVCS_Gear::RegistResponseHandlersetTarget(std::function<void(const eevp::simulation::proxy::methods::setTarget::Output&)> handler)
{
    m_respondersetTarget = handler;
}
 
} /// namespace port
} /// namespace aa
} /// namespace preventrollback
 
/// EOF